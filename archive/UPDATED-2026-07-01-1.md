# UPDATE

## Codex

### v0.142.4 -> v0.142.5

来源：

- GitHub Release：<https://github.com/openai/codex/releases/tag/rust-v0.142.5>
- GitHub Compare：<https://github.com/openai/codex/compare/rust-v0.142.4...rust-v0.142.5>
- npm metadata：`@openai/codex` 的 `latest` 为 `0.142.5`

Release 汇总：

- 上游发布了稳定版 `rust-v0.142.5`，本仓库按既有记录格式归一为 `v0.142.5`。
- 本次是聚焦隐私 / 日志安全的 bug fix：Codex 不再把完整 Responses WebSocket 请求 payload 写入 trace 日志。
- Compare 区间显示 2 个 commit，主要修改 `codex-rs/codex-api/src/endpoint/responses_websocket.rs`，并同步更新 `codex-rs/Cargo.toml`。
- 可见的 `rust-v0.143.0-alpha.32` 属于 prerelease，按 `stable-only` 策略忽略。

破坏性变更：

- 未发现破坏性变更。

迁移说明：

- 未发现迁移步骤或配置变更要求。
- 每日自动化不得把 `v0.142.5` 写回 `ENTRYPOINT.md`；只有用户手动输入 `更新` / `update` 时才允许写回并归档。

对 agent harness workflow 的影响分析：

- 本次修复降低 trace 日志暴露完整请求 payload 的风险，但不改变 Codex 命令、approval、tool search、MCP、插件、sandbox 或 sub-agent 工作流。
- 已用关键词 `Responses`、`WebSocket`、`trace`、`payload`、`日志`、`敏感`、`PII`、`token`、`密钥` 扫描本地规则源：`AGENTS.md`、`kuno-workflow-onboard-skills/SKILL.md`、`kuno-workflow-onboard-skills/REFERENCE.md`、`kuno-workflow-onboard-skills/scripts/onboard.py`、`kuno-workflow-onboard-skills/templates/agents/AGENTS.global.md`、`kuno-workflow-onboard-skills/templates/agents/AGENTS.project.md`、`kuno-workflow-onboard-skills/templates/skills/**`。
- 命中结果显示现有全局 / 项目模板和验证 Skill 已有“不写入真实账号、密钥、PII、生产数据、完整 token、敏感请求头或生产截图 / 日志 / trace / 报告”的通用约束；本次 release 不需要新增长期规则。
- 因没有模板内容变化，已评估根目录 `README.md` / `README.html` 不需要同步更新。

## Trellis

### v0.6.5 -> v0.6.5

来源：

- GitHub Tags：<https://github.com/mindfold-ai/trellis/releases/tag/v0.6.5>
- npm metadata：`@mindfoldhq/trellis` 的 `latest` 为 `0.6.5`，`beta` 为 `0.6.0-beta.23`

Release 汇总：

- 当前稳定版仍为 `v0.6.5`。
- GitHub Releases 仍没有可用 release body；tags 和 npm metadata 均确认 stable 通道没有更新。
- 可见的 `v0.6.0-rc.0`、`v0.6.0-beta.23` 属于非 stable 通道，按 `stable-only` 策略忽略。

破坏性变更：

- 未发现新的 stable 版本，因此未发现新的破坏性变更。

迁移说明：

- 无迁移步骤。

对 agent harness workflow 的影响分析：

- Trellis workflow、Channel、PRD / implement / check gate、`trellis update` 和模板同步边界无变化。
- 本次没有 release-note 支撑的 AGENTS 或 Skill 规则修改。

## GitNexus

### v1.6.8 -> v1.6.8

来源：

- GitHub Tags：<https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.8>
- npm metadata：`gitnexus` 的 `latest` 为 `1.6.8`

Release 汇总：

- 当前稳定版仍为 `v1.6.8`。
- GitHub Releases 没有新的 stable release body；tags 和 npm metadata 均确认 stable 通道没有更新。
- 可见的 `v1.6.9-rc.32` 属于 release candidate，按 `stable-only` 策略忽略。
- npm 包 `@abhigyanpatwari/gitnexus` 返回 404；本轮以既有实际包名 `gitnexus` 为 npm metadata 来源。

破坏性变更：

- 未发现新的 stable 版本，因此未发现新的破坏性变更。

迁移说明：

- 无迁移步骤。

对 agent harness workflow 的影响分析：

- GitNexus MCP / CLI 使用边界、stale index 刷新、分页、PDG / taint / trace opt-in、optional grammar 和大仓库风险规则无变化。
- 本次没有 release-note 支撑的 AGENTS 或 Skill 规则修改。

## Playwright

### v1.61.1 -> v1.61.1

来源：

- GitHub Release：<https://github.com/microsoft/playwright/releases/tag/v1.61.1>
- npm metadata：`playwright` 的 `latest` 为 `1.61.1`，`next` 为 `1.62.0-alpha-2026-06-30`

Release 汇总：

- 当前稳定版仍为 `v1.61.1`。
- npm `next` 通道已有 `1.62.0-alpha-2026-06-30`，但它属于 alpha prerelease，按 `stable-only` 策略忽略。
- `v1.61.1` 仍是最新 stable release，主要是 UI mode、trace viewer、Node loader 和 matcher 相关 bug fix。

破坏性变更：

- 未发现新的 stable 版本，因此未发现新的破坏性变更。

迁移说明：

- 无迁移步骤。

对 agent harness workflow 的影响分析：

- Playwright CLI / MCP 职责边界、项目内安装确认、HTML 报告命名、Markdown 汇总 stem 和 mock-backed 状态规则无变化。
- 本次没有 release-note 支撑的 AGENTS 或 Skill 规则修改。

## Maestro

### cli-2.6.1 -> cli-2.6.1

来源：

- GitHub Release：<https://github.com/mobile-dev-inc/Maestro/releases/tag/cli-2.6.1>
- Raw Changelog：<https://raw.githubusercontent.com/mobile-dev-inc/Maestro/main/CHANGELOG.md>

Release 汇总：

- 当前稳定版仍为 `cli-2.6.1`。
- Changelog 的 `Unreleased` 章节为空，`2.6.1` 仍是最新稳定 CLI 版本。
- `2.6.1` 已知内容仍是 iOS cross-process sheets / hierarchy walk performance、stale iframe handling、CLI hints URL 更新。

破坏性变更：

- 未发现新的 stable 版本，因此未发现新的破坏性变更。

迁移说明：

- 无迁移步骤。

对 agent harness workflow 的影响分析：

- Java 17+ gate、Maestro CLI / MCP 检查顺序、flow 资产路径、`.maestro/reports/` 正式报告和 iOS 真机排障 lesson 规则无变化。
- 本次没有 release-note 支撑的 AGENTS 或 Skill 规则修改。
