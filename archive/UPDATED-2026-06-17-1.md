# UPDATE

## Codex

### v0.140.0 -> v0.140.0

#### 版本判断

- `ENTRYPOINT.md` 当前使用版本为 `v0.140.0`，版本通道策略为 `stable-only`。
- GitHub Releases 中最新可见版本为 `rust-v0.141.0-alpha.4`，属于 prerelease，不纳入 stable-only 比对。
- GitHub Tags 中仍存在稳定 tag `rust-v0.140.0`，未发现高于 `v0.140.0` 的稳定 tag。
- npm `@openai/codex` 的 `latest` dist-tag 为 `0.140.0`，`alpha` dist-tag 为 `0.141.0-alpha.4`。

#### Release 汇总

本次未发现高于 `v0.140.0` 的稳定版 Codex release。由于区间起点和终点相同，没有新增稳定版 release notes 需要汇总；`0.141.0-alpha.4` 仅作为 prerelease 观察记录。

#### 破坏性变更

未发现 accepted stable-channel 新版本，因此没有新的稳定版破坏性变更依据。

#### 迁移说明

不需要迁移。每日自动化不得把 prerelease 或已检测到的最新版本写回 `ENTRYPOINT.md`。

#### 对 agent harness workflow 的影响分析

- 本地规则扫描范围：`AGENTS.md`、`kuno-workflow-onboard-skills/SKILL.md`、`kuno-workflow-onboard-skills/REFERENCE.md`、`kuno-workflow-onboard-skills/scripts/onboard.py`、`kuno-workflow-onboard-skills/templates/agents/*.md`、`kuno-workflow-onboard-skills/templates/skills/**/SKILL.md`。
- 本次 Codex accepted stable-channel 没有新 release，因此不修改 AGENTS 或 Skill 规则。
- 当前 `stable-only` 策略仍适用：忽略 `0.141.0-alpha.4`，等待稳定版 release 后再评估命令、配置、兼容性或 workflow 规则变更。

## Trellis

### v0.6.0 -> v0.6.0

#### 版本判断

- `ENTRYPOINT.md` 当前使用版本为 `v0.6.0`，版本通道策略为 `stable-only`。
- GitHub Releases API 返回空列表，未提供 release body。
- GitHub Tags 中最新稳定 tag 为 `v0.6.0`；同一列表中 `v0.6.0-rc.0` 和 `v0.6.0-beta.23` 均低于或不属于稳定通道。
- npm `@mindfoldhq/trellis` 的 `latest` dist-tag 为 `0.6.0`，`rc` 为 `0.6.0-rc.0`，`beta` 为 `0.6.0-beta.23`。

#### Release 汇总

本次未发现高于 `v0.6.0` 的稳定版 Trellis release。GitHub Releases 仍缺少 release body；稳定版判断依据来自 GitHub Tags 和 npm dist-tags。

#### 破坏性变更

未发现 accepted stable-channel 新版本，因此没有新的稳定版破坏性变更依据。

#### 迁移说明

不需要迁移。`ENTRYPOINT.md` 的 Trellis 当前版本保持 `v0.6.0`。

#### 对 agent harness workflow 的影响分析

- 本地关键词扫描只命中既有 Trellis 安装命令和 stable 策略说明，未命中需要跟随新稳定版调整的配置项。
- 因没有新的稳定版 release notes 或 migration manifest，本次不修改 Trellis workflow、Channel、onboard Skill 或 agent 模板。
- 如果后续 GitHub Releases 继续为空但 tags/npm 出现新稳定版，需要按项目规则补充 compare、commit diff、迁移 manifest 和 npm 包结构分析。

## GitNexus

### v1.6.7 -> v1.6.7

#### 版本判断

- `ENTRYPOINT.md` 当前使用版本为 `v1.6.7`，版本通道策略为 `stable-only`。
- GitHub Releases 中最新可见版本为 `v1.6.8-rc.48`，标记为 prerelease，不纳入 stable-only 比对。
- GitHub Tags 中最新稳定 tag 仍为 `v1.6.7`。
- npm `gitnexus` 的 `latest` dist-tag 为 `1.6.7`，`rc` dist-tag 为 `1.6.8-rc.48`。

#### Release 汇总

本次未发现高于 `v1.6.7` 的稳定版 GitNexus release。`v1.6.8-rc.48` 的 release notes 提到 multi-branch indexing、branch-scoped querying、CFG/PDG/taint、Streamable HTTP MCP、embedding 参数、route impact 修复和大仓库性能改进等内容，但这些仍处于 rc 通道，仅作为观察记录，不作为本仓库长期规则更新依据。

#### 破坏性变更

未发现 accepted stable-channel 新版本，因此没有新的稳定版破坏性变更依据。

#### 迁移说明

不需要迁移。稳定用户仍应使用 npm `latest` 对应的 `1.6.7`；`gitnexus@rc` 不应被本仓库每日自动化提升为默认建议。

#### 对 agent harness workflow 的影响分析

- 本地关键词扫描未发现需要因 accepted stable-channel 新版本立即修改的 GitNexus MCP、索引、影响分析或变更检测规则。
- GitNexus rc release notes 中的 branch-scoped querying、HTTP MCP、CFG/PDG/taint 等概念可能影响未来规则，但在 stable-only 策略下，本次不沉淀为长期 AGENTS 或 Skill 规则。
- 当 `1.6.8` 正式进入 stable/latest 后，需要重新评估 GitNexus MCP 使用边界、分支索引查询、HTTP transport、数据流分析和大仓库索引风险说明。
