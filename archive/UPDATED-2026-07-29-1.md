# UPDATE

## Codex v0.145.0 -> v0.146.0

- **检测结论**：`openai/codex` 的最新 non-draft、non-prerelease GitHub Release 为 `rust-v0.146.0`（2026-07-29 发布）；按本仓库规范化规则比较后为 `v0.146.0`。`rust-v0.145.0...rust-v0.146.0` 是本次唯一 stable 区间。
- **变更汇总**：新增会话命名、固定与旁支会话；新增 Agent Plugins manifest、工作区发布及 Amazon Bedrock / Claude Code marketplace；新增兼容自定义模型提供方的独立 Web Search；可发现 executor 提供的 Skill 并安全读取其关联资源。MCP 在认证或配置变更后可保持健康连接并重连已关闭服务器；skill catalog 在上下文预算紧张时保留更多条目并报告截断。
- **破坏性变更与迁移**：release notes 未声明破坏性变更或迁移步骤。版本化 prompt 与规则不应据此假设 plugin、marketplace、MCP 或 executor Skill 已安装、已授权或当前可调用。
- **证据与本地影响**：GitHub Release 页面及其 `rust-v0.145.0...rust-v0.146.0` 完整 changelog 均可读。以 `plugin`、`plugin manifest`、`plugin marketplace`、`executor-provided skill`、`selected skill`、`skill resource`、`MCP connection`、`skill catalog`、`context budget` 扫描受限范围：`AGENTS.global.md` 已规定工具列表 / `tool_search` / MCP 可见性才构成可用性证据，marketplace 或已安装提示不是授权或可调用证明，且禁止因上游能力静默改写配置；Onboard 文档和脚本仅含既有的 MCP 配置及 external Skill 安全安装契约。现有长期规则已覆盖本次影响，不修改 AGENTS 或 Skill 模板。

## Trellis v0.6.9 -> v0.6.10

- **检测结论**：`mindfold-ai/trellis` 的 GitHub Releases API 为空；GitHub Tags 的最新 stable tag 为 `v0.6.10`，`v0.7.0-beta.1` 为 prerelease，不纳入 stable-only 区间。
- **变更汇总**：`v0.6.9...v0.6.10` compare 显示 3 个提交。修复 Python 3.9–3.11 任务脚本的嵌套 f-string 兼容性；当 Codex `SubagentStart.additionalContext` 截断并给出 `Full hook output saved to: <path>` 时，`trellis-{implement,check,research}` 先恢复保存的完整输出，失败再使用各自 pull fallback；`task.py finish` 只清除提供已解析 active task 的 fallback session，不猜测或批量删除其他 session。
- **破坏性变更与迁移**：目标 tag 的 migration manifest 标记 `breaking: false`、`recommendMigrate: false`；应运行 `trellis update` 刷新任务脚本与 Codex 子代理模板，不需要 `trellis update --migrate`。
- **证据与本地影响**：release body 缺失；已读取目标 tag、GitHub compare、修复提交及 36 个文件变更、`packages/cli/src/migrations/manifests/0.6.10.json`、tagged npm package metadata 和 npm registry tarball metadata。根 `CHANGELOG.md` 在目标 tag 不存在。以 `Python 3.9`、`SubagentStart`、`fallback session`、`task.py finish`、`trellis update`、`--migrate`、`Full hook output saved`、`context injection`、`dispatch_mode` 及三个 role 名称扫描受限范围；现有 `trellis-workflow` 已要求保留 bounded injection、优先读取 saved output、运行 `trellis update` 并复核三份生成 profile。该版本是既有 Codex dispatch / hook 流程的修复，不改变 `codex.dispatch_mode` 默认值，也不适用于 OMP 的调度规则；不修改长期模板。

## GitNexus v1.6.9 -> v1.6.9

- **检测结论**：`abhigyanpatwari/GitNexus` 的 `releases/latest` 仍为 `v1.6.9`；Tags 首页中的 `v1.6.10-rc.*` 均为 prerelease。stable-only 策略不跨入 RC 通道。
- **依据与影响**：当前 stable 基线与最新 stable release 相同；无需新增 release 汇总、迁移或本地规则变更。

## Playwright v1.62.0 -> v1.62.0

- **检测结论**：`microsoft/playwright` 的最新 non-draft、non-prerelease GitHub Release 为 `v1.62.0`（2026-07-24），与基线一致。
- **依据与影响**：release body 可读；没有新的 stable 区间、破坏性变更或迁移要求，因此不修改测试工具规则。

## Maestro cli-2.7.0 -> cli-2.7.0

- **检测结论**：`mobile-dev-inc/Maestro` 的最新 non-draft、non-prerelease GitHub Release 为 `cli-2.7.0`（2026-07-20），与基线一致。
- **依据与影响**：release body 指向上游 `CHANGELOG.md#270`；没有新的 stable 区间、破坏性变更或迁移要求，因此不修改 Mobile / Hybrid E2E 规则。
