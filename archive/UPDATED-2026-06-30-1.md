# UPDATE

## Codex

### v0.142.3 -> v0.142.4

#### 版本检测

- 检测来源：GitHub Releases、GitHub tags、GitHub compare、npm registry。
- 最新稳定版：`rust-v0.142.4` / npm `@openai/codex@0.142.4`。
- `stable-only` 策略下，本轮忽略 `rust-v0.143.0-alpha.31` 和 npm `alpha` / platform alpha dist-tag。
- GitHub release 发布时间为 2026-06-29；`ENTRYPOINT.md` 中固定起点为 `v0.142.3`。

#### Release 汇总

- 上游 release 将本版本归类为维护发布，没有标注用户可见功能变化。
- `rust-v0.142.3...rust-v0.142.4` compare 显示 3 个提交，主要涉及：
  - 恢复 v1 delegation guidance。
  - 回退 auto-review on-request prompt 的主动化改动。
  - 版本 / chore 更新。
- 变更文件集中在 Codex Rust 侧的 multi-agent spec、search tool 测试、spawn agent description 测试、permissions prompt 和 approval policy prompt 模板。

#### 破坏性变更

- 未发现面向 agent harness 使用者的破坏性变更。
- release notes 没有要求调整 CLI 命令、配置格式、MCP 配置或插件安装方式。

#### 迁移说明

- 本仓库不写回 `ENTRYPOINT.md`；只有用户手动输入 `更新` / `update` 后才允许把当前版本推进到 `v0.142.4`。
- 日常使用如果选择升级 Codex，可按既有 Codex 更新方式处理；本次 release 没有额外迁移步骤。

#### 对 agent harness workflow 的影响分析

- 本地关键词扫描覆盖：`AGENTS.md`、`kuno-workflow-onboard-skills/SKILL.md`、`REFERENCE.md`、`scripts/onboard.py`、当前 agents 模板和 `templates/skills/**`。
- 命中项主要集中在 Trellis Channel / worker / spawn / multi-agent 边界，以及通用 permissions 文案；这些规则已经要求不得静默 spawn worker、不得混用 Trellis Channel 与 Codex 内置 multi-agent、不得绕过 approval / permission 边界。
- 由于本次 Codex release 明确无用户可见变化，且 compare 没有引入新的 agent harness 命令、配置项或兼容性边界，本轮不修改 `AGENTS.md`、agents 模板、onboard 脚本或 Skill 模板。
- README / README.html 同步评估：无需更新，因为本轮没有改变工作流主线、工具职责边界、验证工具栈、模板路径或 onboard 行为。

## Trellis

### v0.6.5 -> v0.6.5

#### 版本检测

- 检测来源：GitHub Releases、GitHub tags、npm registry。
- GitHub Releases 仍为空；GitHub tags 和 npm `@mindfoldhq/trellis` `latest` dist-tag 均确认最新稳定版为 `v0.6.5`。
- `stable-only` 策略下，本轮忽略 `v0.6.0-rc.0`、`v0.6.0-beta.23` 等 prerelease 通道。
- `ENTRYPOINT.md` 中固定起点为 `v0.6.5`，因此本轮无跨版本稳定更新区间。

#### Release 汇总

- 没有发现高于 `v0.6.5` 的新稳定 release 或 tag。
- 因 GitHub Releases 没有新稳定 release body，本轮以 tags 和 npm metadata 作为“无新增稳定版”的依据。

#### 破坏性变更

- 未发现新增稳定版破坏性变更。

#### 迁移说明

- 无迁移动作。
- 不修改 `ENTRYPOINT.md`，不执行 `trellis update`，不归档更新记录。

#### 对 agent harness workflow 的影响分析

- 没有新的稳定 release-note 依据触发 Trellis workflow、Channel、配置或迁移规则变更。
- 本仓库 `.trellis/` 和 `.trellis/workflow.md` 不存在，Trellis 阶段按规则跳过。
- README / README.html 同步评估：无需更新。

## GitNexus

### v1.6.8 -> v1.6.8

#### 版本检测

- 检测来源：GitHub Releases、GitHub tags、npm registry。
- npm `gitnexus` `latest` dist-tag 仍为 `1.6.8`，GitHub 稳定 release `v1.6.8` 仍是当前稳定版。
- `stable-only` 策略下，本轮忽略 `v1.6.9-rc.28` 等 release candidate；npm `rc` dist-tag 为 `1.6.9-rc.28`。
- `ENTRYPOINT.md` 中固定起点为 `v1.6.8`，因此本轮无跨版本稳定更新区间。

#### Release 汇总

- 没有发现高于 `v1.6.8` 的新稳定 release。
- 当前稳定版 `v1.6.8` 的既有重点仍是 opt-in PDG-backed impact、taint analysis、多分支索引、private repo cloning、MCP `trace` tool 和 Streamable HTTP MCP server；这些内容已在现有全局 / 项目规则中有长期边界说明。
- 新的 `v1.6.9-rc.*` release candidate 包含 HTTP route / consumer、PDG trace、taint source / sink、MCP tolerance 等持续迭代，但按 `stable-only` 不推进本轮稳定区间。

#### 破坏性变更

- 未发现新增稳定版破坏性变更。

#### 迁移说明

- 无迁移动作。
- 本仓库未建立 `.gitnexus/` 索引；GitNexus MCP / index 检查按规则跳过。

#### 对 agent harness workflow 的影响分析

- 现有规则已经覆盖 stale index、分页、多分支索引、PDG / taint / trace opt-in、MCP transport、HTTP route / consumer 交叉核对等边界。
- 由于没有新的稳定 release-note 依据，本轮不改 GitNexus 相关 AGENTS 或 Skill 规则。
- README / README.html 同步评估：无需更新。

## Playwright

### v1.61.1 -> v1.61.1

#### 版本检测

- 检测来源：GitHub Releases、GitHub tags、npm registry。
- GitHub Releases、GitHub tags 和 npm `playwright` `latest` dist-tag 均确认最新稳定版为 `v1.61.1`。
- `stable-only` 策略下，本轮忽略 npm `next` dist-tag `1.62.0-alpha-2026-06-29` 和 `beta` dist-tag。
- `ENTRYPOINT.md` 中固定起点为 `v1.61.1`，因此本轮无跨版本稳定更新区间。

#### Release 汇总

- 没有发现高于 `v1.61.1` 的新稳定 release。
- 当前稳定版 `v1.61.1` 的既有修复集中在 expect matcher override、UI mode APIRequestContext 字节统计、Trace Viewer WebSocket 时间、Node 22.15 sync loader regression、pnpm workspace extensionless TS import resolution。

#### 破坏性变更

- 未发现新增稳定版破坏性变更。

#### 迁移说明

- 无迁移动作。
- 不修改 Playwright CLI / MCP 的现有检测、安装或 fallback 规则。

#### 对 agent harness workflow 的影响分析

- 没有新的稳定 release-note 依据触发 Web 验证职责边界、报告路径、Playwright CLI 检测顺序或 MCP 边界更新。
- README / README.html 同步评估：无需更新。

## Maestro

### cli-2.6.1 -> cli-2.6.1

#### 版本检测

- 检测来源：GitHub Releases、GitHub tags、raw CHANGELOG。
- GitHub Releases 最新稳定 CLI 仍为 `cli-2.6.1`；tags 最新稳定对应 `v2.6.1`。
- raw CHANGELOG 的 `Unreleased` 章节为空。
- `ENTRYPOINT.md` 中固定起点为 `cli-2.6.1`，因此本轮无跨版本稳定更新区间。

#### Release 汇总

- 没有发现高于 `cli-2.6.1` 的新稳定 release。
- 当前稳定版 `2.6.1` changelog 重点仍是 iOS cross-process sheets / hierarchy walk performance、cross-origin iframe stale handling，以及 CLI hints URL 更新。

#### 破坏性变更

- 未发现新增稳定版破坏性变更。

#### 迁移说明

- 无迁移动作。
- 不修改 Java 17+ gate、Maestro CLI / MCP 检测顺序、flow 资产路径或报告路径。

#### 对 agent harness workflow 的影响分析

- 没有新的稳定 release-note 依据触发 Mobile / Hybrid E2E、Maestro flow、真机排障 lesson 或报告命名规则变更。
- README / README.html 同步评估：无需更新。
