# UPDATE

## Codex

### v0.137.0 -> v0.137.0

- 最新检测结果：`ENTRYPOINT.md` 当前版本为 `v0.137.0`，GitHub Releases 仍将 `0.137.0` / `rust-v0.137.0` 标记为最新稳定版本；npm `@openai/codex` 的 `latest` dist-tag 也是 `0.137.0`。
- 版本通道处理：当前策略为 `stable-only`。检测到 `0.138.0-alpha.1` 至 `0.138.0-alpha.6`，其中 `0.138.0-alpha.6` 是最新 alpha，但属于预发布通道，本次不纳入更新区间。
- Release 汇总：固定起点和终点相同，本次没有新增稳定版 release notes 需要从 `v0.137.0` 汇总到更高稳定版本。
- 破坏性变更：未发现适用于稳定通道的新破坏性变更。
- 迁移说明：无需迁移；不要在每日自动化中把 alpha 版本写回 `ENTRYPOINT.md`。
- 对 agent harness workflow 的影响分析：现有 Codex 规则已覆盖插件、权限、远程控制、多 agent 和工具执行边界；本次没有稳定版新增内容要求修改 `AGENTS.md`、`agents/AGENTS.global.md`、`agents/AGENTS.project.md` 或 `skills/**/SKILL.md`。
- 依据来源：GitHub Releases `https://github.com/openai/codex/releases`；npm dist-tags `@openai/codex`。

## Trellis

### v0.6.0-beta.22 -> v0.6.0-beta.22

- 最新检测结果：`ENTRYPOINT.md` 当前版本为 `v0.6.0-beta.22`；GitHub Releases 页面没有发布 release 条目，GitHub Actions 中最新同 beta 通道 npm 发布仍为 `v0.6.0-beta.22`，npm `@mindfoldhq/trellis` 的 `beta` dist-tag 也是 `0.6.0-beta.22`。
- 版本通道处理：当前策略为 `same-prerelease-channel`。当前版本属于 `v0.6.0-beta.x` 通道，本次没有发现更高的 `v0.6.0-beta.x` 版本；稳定 `latest` 通道的 `0.5.19` 不作为本次目标。
- Release 汇总：固定起点和终点相同，且 GitHub Releases 为空，本次没有新增同 beta 通道 release notes 需要汇总。
- 破坏性变更：未发现适用于 `v0.6.0-beta.x` 同通道的新破坏性变更。
- 迁移说明：无需运行 `trellis update` 或其他迁移动作；每日自动化不更新 `ENTRYPOINT.md`。
- 对 agent harness workflow 的影响分析：现有 Trellis 规则已覆盖 Trellis 强证据判断、`.trellis/workflow.md` 读取、Channel 使用边界和 Codex multi-agent 配置兼容性；本次没有新同通道版本要求修改长期规则或 Skill。
- 依据来源：GitHub Releases `https://github.com/mindfold-ai/Trellis/releases`；GitHub Actions `https://github.com/mindfold-ai/Trellis/actions`；npm dist-tags `@mindfoldhq/trellis`。

## GitNexus

### V1.6.5 -> V1.6.5

- 最新检测结果：`ENTRYPOINT.md` 当前版本为 `V1.6.5`；GitHub 仓库仍显示稳定最新版本为 `v1.6.5`，npm `gitnexus` 的 `latest` dist-tag 也是 `1.6.5`。
- 版本通道处理：当前策略为 `stable-only`。GitHub Releases 可见 `v1.6.6-rc.141` 至 `v1.6.6-rc.153` 等 release candidate；npm `rc` dist-tag 已到 `1.6.6-rc.154`。这些均为预稳定版本，本次不纳入更新区间。
- Release 汇总：固定起点和终点相同，本次没有新增稳定版 release notes 需要从 `V1.6.5` 汇总到更高稳定版本。
- 破坏性变更：未发现适用于稳定通道的新破坏性变更。
- 迁移说明：无需迁移；不要把 rc 通道写回 `ENTRYPOINT.md` 的当前使用版本。
- 对 agent harness workflow 的影响分析：当前 AGENTS 规则已要求仅在 GitNexus MCP 可用且当前项目已有索引时使用 GitNexus，并把它作为影响分析和变更验证辅助；本次没有稳定版新增内容要求修改 `AGENTS.md`、`agents/AGENTS.global.md`、`agents/AGENTS.project.md` 或 `skills/**/SKILL.md`。
- 依据来源：GitHub Releases `https://github.com/abhigyanpatwari/GitNexus/releases`；GitHub 仓库首页 `https://github.com/abhigyanpatwari/GitNexus`；npm dist-tags `gitnexus`。
