# UPDATE

## Codex

### v0.139.0 -> v0.139.0

### 检测依据

- `ENTRYPOINT.md` 当前使用版本为 `v0.139.0`，版本通道策略为 `stable-only`。
- GitHub Releases API 显示最新稳定发布仍为 `rust-v0.139.0` / `0.139.0`，发布时间为 2026-06-09T20:13:29Z。
- GitHub Releases 中可见 `rust-v0.140.0-alpha.8`、`rust-v0.140.0-alpha.9`、`rust-v0.140.0-alpha.11`、`rust-v0.140.0-alpha.13`、`rust-v0.140.0-alpha.14` 等预发布版本；npm `@openai/codex` 的 `alpha` dist-tag 也指向 `0.140.0-alpha.14`。
- npm `@openai/codex` 的 `latest` dist-tag 和包版本均为 `0.139.0`。

### Release 汇总

本次 `stable-only` 通道没有发现高于 `v0.139.0` 的稳定版本，因此没有新的稳定版 release notes 需要追加汇总。`0.140.0-alpha.*` 属于预发布通道，最新观察到 `0.140.0-alpha.14`，本次按策略忽略，不进入 `UPDATE.md` 目标版本。

### 破坏性变更

未发现本次可接受通道内新增稳定版本，因此没有新的破坏性变更结论。预发布通道中即使出现实现调整，也不作为当前稳定版自动化规则更新依据。

### 迁移说明

无需迁移；`ENTRYPOINT.md` 不应在每日自动化中写回，仍保持 `v0.139.0`。

### 对 agent harness workflow 的影响分析

本次没有新的稳定版目标版本。已用与当前 release / 预发布相关的关键词扫描根 `AGENTS.md`、`kuno-workflow-onboard-skills/` 下的 agent 模板、Skill 模板、onboard Skill、参考文档和安装脚本，未发现需要根据可接受通道 release notes 最小化修改的长期规则。Codex 预发布内容不写入长期规则，避免把未进入稳定通道的行为边界沉淀到可复用模板。

## Trellis

### v0.6.0-beta.23 -> v0.6.0-beta.23

### 检测依据

- `ENTRYPOINT.md` 当前使用版本为 `v0.6.0-beta.23`，版本通道策略为 `same-prerelease-channel`。
- GitHub Releases API 返回空列表；因此本次以 GitHub Tags API 和 npm metadata 交叉确认。
- GitHub Tags API 显示 `v0.6.0-rc.0`、`v0.6.0-beta.23`、`v0.6.0-beta.22` 等标签，其中当前同一 beta 通道内最新仍为 `v0.6.0-beta.23`。
- npm `@mindfoldhq/trellis` 的 `beta` dist-tag 为 `0.6.0-beta.23`，`rc` dist-tag 为 `0.6.0-rc.0`，`latest` dist-tag 为 `0.5.19`。

### Release 汇总

本次没有发现高于 `v0.6.0-beta.23` 的同一 `v0.6.0-beta.x` 通道版本。`v0.6.0-rc.0` / npm `rc` 属于不同 prerelease 通道；根据 `same-prerelease-channel` 策略，本次不主动从 beta 跳到 rc，也不把 rc 内容写为目标版本。

### 破坏性变更

没有新的同通道 beta release notes，因此没有新的破坏性变更结论。GitHub Releases 仍为空，缺少可直接追溯的 release body；本次通过 Tags API 和 npm dist-tags 确认版本状态。

### 迁移说明

无需迁移；`ENTRYPOINT.md` 继续作为只读基线，不写回 `v0.6.0-rc.0`。

### 对 agent harness workflow 的影响分析

本仓库已有 Trellis beta.23 相关长期规则，覆盖 registry-backed spec templates、`trellis update` diff 复核和 Channel runtime agent 定义。由于本次未发现新的同 beta 通道版本，且 rc 通道不在本次比较范围内，因此不修改 `AGENTS.md`、agent 模板或 Trellis Skill 模板。

## GitNexus

### v1.6.7 -> v1.6.7

### 检测依据

- `ENTRYPOINT.md` 当前使用版本为 `v1.6.7`，版本通道策略为 `stable-only`。
- npm `gitnexus` 的 `latest` dist-tag 和包版本均为 `1.6.7`。
- npm `gitnexus` 的 `rc` dist-tag 为 `1.6.8-rc.18`；GitHub Releases / Tags 中也可见 `v1.6.8-rc.*` 预发布版本，最新观察到 `v1.6.8-rc.18`。
- GitHub `v1.6.8-rc.*` release notes 标明这些版本是 release candidate，稳定版本仍在 `latest` dist-tag。

### Release 汇总

本次 `stable-only` 通道没有发现高于 `v1.6.7` 的稳定版本。`v1.6.8-rc.*` 包含多分支索引、branch-scoped 查询、Java Spring route annotation 提取、`impact()` / `route_map` 覆盖范围修复、VECTOR index 创建路径修复、pnpm strict 下 `onnxruntime-common` 解析修复、非可克隆 worker 结果容错、vendored tree-sitter grammar 绝对路径加载、临时 I/O 错误下避免 registry wipe、TS/JS control-flow graph layer、intra-procedural REACHING_DEF 数据依赖层、hook db-lock probe 边界等预发布内容，但这些仍属于 rc 通道，本次不纳入目标版本。

### 破坏性变更

没有新的稳定版 release notes，因此没有新的破坏性变更结论。`v1.6.8-rc.*` 可能影响索引、查询、依赖解析、grammar 加载、存储容错和 hook 行为，但在稳定版发布前只作为后续观察项。

### 迁移说明

无需迁移；每日自动化不把 rc 内容写回 `ENTRYPOINT.md`，也不要求用户升级。

### 对 agent harness workflow 的影响分析

本仓库已在 agent 模板中沉淀 GitNexus `list_repos` 分页、`gitnexus uninstall` dry-run / `--force` 安全边界、可选 grammar / prebuild 覆盖风险、大仓库索引完整性风险等长期规则。`v1.6.8-rc.*` 的多分支索引、route / impact 修复、CFG / reaching-def 层、grammar 加载和 hook 行为修复仍属于预发布内容，本次只记录观察，不更新可复用规则；待稳定版进入 `latest` 后再根据 release notes 最小化调整。
