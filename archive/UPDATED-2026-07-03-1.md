# UPDATE

## Codex

### v0.142.5 -> v0.142.5

来源：

- GitHub Releases：`openai/codex` 最新稳定 release 仍为 `rust-v0.142.5`。
- GitHub Tags：最新稳定 tag 仍为 `rust-v0.142.5`，较新的 `rust-v0.143.0-alpha.35`、`rust-v0.143.0-alpha.34`、`rust-v0.143.0-alpha.33`、`rust-v0.143.0-alpha.32`、`rust-v0.143.0-alpha.31` 为 prerelease。
- npm metadata：`@openai/codex` 的 `latest` 仍为 `0.142.5`，`alpha` 为 `0.143.0-alpha.35`。

Release 汇总：

- 本次没有发现高于 `ENTRYPOINT.md` 基线 `v0.142.5` 的新稳定版。
- `v0.142.5` 的可追溯 release note 仍是 Responses WebSocket trace 日志隐私修复：避免完整请求 payload 被写入 trace log。
- `0.143.0-alpha.35` 属于 alpha 通道；当前策略是 `stable-only`，不作为本次目标版本。

破坏性变更：

- 本次稳定通道无新增破坏性变更。

迁移说明：

- 无需迁移；不要把 alpha 通道推进到 `ENTRYPOINT.md`。

对 agent harness workflow 的影响分析：

- 本地规则扫描命中已有 trace、日志、payload、敏感数据和隐私边界规则；现有规则已经覆盖该稳定版修复表达的风险。
- 没有发现需要修改 `AGENTS.md`、`kuno-workflow-onboard-skills/templates/agents/*.md`、`kuno-workflow-onboard-skills/SKILL.md`、`REFERENCE.md`、`scripts/onboard.py` 或 Skill 模板的新增稳定 release 依据。

## Trellis

### v0.6.5 -> v0.6.5

来源：

- GitHub Releases：`mindfold-ai/trellis` 当前仍没有 GitHub release 条目。
- GitHub Tags：最新稳定 tag 仍为 `v0.6.5`。
- npm metadata：`@mindfoldhq/trellis` 的 `latest` 仍为 `0.6.5`，`beta` 为 `0.6.0-beta.23`，`rc` 为 `0.6.0-rc.0`。
- GitHub compare：`v0.6.4...v0.6.5` 可追溯到 28 个 commits 和 30 个文件变化。
- 官方 changelog/docs 直连检查：`CHANGELOG.md`、`docs/changelog.md`、`docs/changelog/v0.6.5.md`、`docs/zh/changelog/v0.6.5.md` 均返回 404。
- npm 包 metadata：`0.6.5` 发布包提供 `trellis` / `tl` bin，核心依赖包括 `@mindfoldhq/trellis-core`。

Release 汇总：

- 本次没有发现高于 `ENTRYPOINT.md` 基线 `v0.6.5` 的新稳定版。
- 因 GitHub Releases 和公开 changelog 缺失，本次继续用 GitHub tags、npm `latest` 和 GitHub compare 交叉确认。
- `v0.6.4...v0.6.5` 的主要可追溯变化包括 Trae IDE 平台支持、Pi / subagent tools frontmatter 支持、Windows channel spawn `.exe` 路径修复、hook 空 stdin 不阻塞、channel messages 相关修改、workflow / platform integration 文档和任务产物更新。
- `0.6.0-beta.23` 与 `0.6.0-rc.0` 不属于 stable-only 目标；当前基线已经是更高的稳定版 `0.6.5`。

破坏性变更：

- 本次稳定通道无新增破坏性变更。
- 因上游没有 release body 和公开 changelog，Trellis 的细粒度变更解释仍依赖 compare / commit message / npm metadata，完整语义存在来源不完整风险。

迁移说明：

- 无需迁移；不要执行 `trellis init`，也不要把 prerelease 通道写回 `ENTRYPOINT.md`。

对 agent harness workflow 的影响分析：

- 本地规则已经包含 Trellis `.trellis/` 可用性判定、`trellis update` 风险、Channel runtime、worker 边界、Windows / platform 相关通用约束和 MCP / validation 边界。
- 今日没有新的稳定 release note 要求修改本仓库长期规则或 Skill 模板。

## GitNexus

### v1.6.8 -> v1.6.8

来源：

- GitHub Releases：`abhigyanpatwari/GitNexus` 最新稳定 release 仍为 `v1.6.8`。
- GitHub Tags：最新稳定 tag 仍为 `v1.6.8`。
- npm metadata：`gitnexus` 的 `latest` 仍为 `1.6.8`，`rc` 为 `1.6.9-rc.44`。

Release 汇总：

- 本次没有发现高于 `ENTRYPOINT.md` 基线 `v1.6.8` 的新稳定版。
- `v1.6.8` 的既有 release note 仍是 PDG-backed impact analysis、taint analysis、多分支索引、私有仓库 clone、Streamable HTTP MCP server 等能力；这些能力默认或按需 opt-in 的边界已在本地规则中存在。
- `v1.6.9-rc.44` 属于 RC 通道；当前策略是 `stable-only`，不作为本次目标版本。

破坏性变更：

- 本次稳定通道无新增破坏性变更。

迁移说明：

- 无需迁移；不要把 RC 通道写回 `ENTRYPOINT.md`。

对 agent harness workflow 的影响分析：

- 本地规则扫描命中已有 PDG、taint、trace、多分支索引、MCP transport、stale index、optional grammar 和大仓库风险规则。
- 没有发现需要新增或调整 GitNexus 使用边界的稳定 release 依据。

## Playwright

### v1.61.1 -> v1.61.1

来源：

- GitHub Releases：`microsoft/playwright` 最新稳定 release 仍为 `v1.61.1`。
- GitHub Tags：最新稳定 tag 仍为 `v1.61.1`。
- npm metadata：`playwright` 的 `latest` 仍为 `1.61.1`，`next` 为 `1.62.0-alpha-2026-07-02`，`beta` 为 `1.61.1-beta-1782889362000`。

Release 汇总：

- 本次没有发现高于 `ENTRYPOINT.md` 基线 `v1.61.1` 的新稳定版。
- `v1.61.1` 仍是 bug fix release，覆盖 expect matcher 覆盖默认 matcher、UI mode API request bytes 报告、trace viewer WebSocket message time scale、Node 22.15 sync loader、pnpm workspace symlink 下 extensionless `.ts` subpath import 等问题。
- `1.62.0-alpha-2026-07-02` 属于 next alpha 通道，`1.61.1-beta-1782889362000` 属于 beta 通道；当前策略是 `stable-only`，不作为本次目标版本。

破坏性变更：

- 本次稳定通道无新增破坏性变更。

迁移说明：

- 无需迁移；继续以项目内 Playwright CLI / `@playwright/test` 作为 Web E2E 执行底座。

对 agent harness workflow 的影响分析：

- 本地规则已经覆盖 Playwright CLI 与 Playwright MCP 的职责边界、项目级安装确认、HTML reporter 临时目录与正式报告快照分离、Markdown 汇总 stem 规则和失败后重跑顺序。
- 今日没有新的稳定 release 依据要求修改 Playwright 验证流程或报告规则。

## Maestro

### cli-2.6.1 -> cli-2.6.1

来源：

- GitHub Releases：`mobile-dev-inc/Maestro` 最新稳定 CLI release 仍为 `cli-2.6.1`。
- GitHub Tags：稳定 tag 列表仍包含 `v2.6.1` / `cli-2.6.1` 作为最新稳定版本。
- Raw changelog：`CHANGELOG.md` 的 `Unreleased` 为空，最新条目仍为 `2.6.1`。

Release 汇总：

- 本次没有发现高于 `ENTRYPOINT.md` 基线 `cli-2.6.1` 的新稳定版。
- `2.6.1` 的既有 changelog 仍是 iOS cross-process sheets 与 hierarchy walk 性能修复、`fetchCrossOriginIframeContent` stale iframe 处理、CLI 提示 URL 从 legacy 地址更新到 `app.maestro.dev`。

破坏性变更：

- 本次稳定通道无新增破坏性变更。

迁移说明：

- 无需迁移；继续按 Java 17+ -> Maestro CLI -> Maestro MCP 的检测顺序处理移动 / Hybrid E2E。

对 agent harness workflow 的影响分析：

- 本地规则已经覆盖 Java 17+ gate、Maestro CLI / MCP 边界、flow 资产路径、正式报告与 Markdown 汇总路径、iOS 真机排障 lesson 懒加载和 Web smoke 与 Web 回归职责区分。
- 今日没有新的稳定 release 依据要求修改 Maestro 规则或 `maestro-mobile-e2e` Skill。

