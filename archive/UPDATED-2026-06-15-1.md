# UPDATE

## Codex

### v0.139.0 -> v0.139.0

检测结论：`ENTRYPOINT.md` 中 Codex 当前版本为 `v0.139.0`，版本通道策略为 `stable-only`。本次 GitHub Releases 列表中最新可见版本为 `rust-v0.140.0-alpha.19`，但该版本标记为 prerelease；GitHub 当前稳定 tag `rust-v0.139.0` 仍可按 tag 读取，npm `@openai/codex` 的 `latest` dist-tag 仍为 `0.139.0`，`alpha` dist-tag 为 `0.140.0-alpha.19`。因此本次 accepted-channel 终点仍为 `v0.139.0`，不得把 alpha 写回 `ENTRYPOINT.md`。

来源依据：

- GitHub Releases API：可见 `rust-v0.140.0-alpha.19`，`prerelease: true`，发布日期为 2026-06-14。
- GitHub release-by-tag：`https://github.com/openai/codex/releases/tag/rust-v0.139.0` 可读取，`prerelease: false`，发布日期为 2026-06-09。
- npm metadata：`@openai/codex` 的 `latest` 为 `0.139.0`，`alpha` 为 `0.140.0-alpha.19`，平台包 dist-tags 仍对齐 `0.139.0`。

Release 汇总：本次从 `v0.139.0` 到 `v0.139.0` 没有新的稳定版 release 需要汇总。`0.140.0-alpha.*` 系列 release body 只有 alpha 发布说明，属于 prerelease 观察项，不进入 stable-only 汇总。

破坏性变更：无新的稳定版破坏性变更。

迁移说明：无需迁移；每日自动化不写回 `ENTRYPOINT.md`，也不建议用户在 stable-only 监控项中切换到 alpha。

对 agent harness workflow 的影响分析：已用 `0.140.0-alpha`、`web search`、`oneOf`、`allOf`、`codex doctor`、`schema`、`MCP`、`connector` 等关键词扫描 `AGENTS.md` 与 `kuno-workflow-onboard-skills/`。命中项均为既有 MCP / connector / schema 边界说明或当前稳定版本已经覆盖的通用规则；本次没有 accepted-channel 新能力、命令、配置或兼容性变更，因此不修改 AGENTS 或 Skill 模板。

## Trellis

### v0.6.0-beta.23 -> v0.6.0-beta.23

检测结论：`ENTRYPOINT.md` 中 Trellis 当前版本为 `v0.6.0-beta.23`，版本通道策略为 `same-prerelease-channel`。GitHub Releases API 仍返回空列表；GitHub tags 列表端点本次返回 504，但 `matching-refs/tags/v0.6.0-beta` 成功返回 `v0.6.0-beta.0` 到 `v0.6.0-beta.23`，同一 beta 通道最高版本仍为 `v0.6.0-beta.23`。同时可见 `v0.6.0-rc.0`，npm `@mindfoldhq/trellis` 的 `rc` dist-tag 也为 `0.6.0-rc.0`，但 rc 不属于当前 beta 通道，按策略不主动跳转。

来源依据：

- GitHub Releases API：返回空数组，没有 release body。
- GitHub tags API：本次 `tags?per_page=20/60` 返回 504，因此未作为唯一依据。
- GitHub matching refs API：`refs/tags/v0.6.0-beta.23` 存在，sha 为 `9d63f90fb5beb2df39180c07cb260bca1db29da4`；`refs/tags/v0.6.0-rc.0` 也存在，但属于 rc 通道。
- npm metadata：`@mindfoldhq/trellis` 的 `beta` 为 `0.6.0-beta.23`，`rc` 为 `0.6.0-rc.0`，`latest` 为 `0.5.19`。

Release 汇总：本次从 `v0.6.0-beta.23` 到 `v0.6.0-beta.23` 没有新的同 beta 通道版本需要汇总。GitHub release body 缺失且 tags 列表端点不稳定，已用 matching refs 与 npm dist-tags 交叉确认。

破坏性变更：无新的同 beta 通道破坏性变更。`v0.6.0-rc.0` 未纳入本次比较终点。

迁移说明：无需执行 `trellis update` 或迁移；每日自动化不应从 beta 通道跳到 rc 通道。

对 agent harness workflow 的影响分析：已用 `0.6.0-rc`、`beta`、`rc`、`trellis update`、`Channel`、`runtime agent`、`workflow`、`spec` 等关键词扫描 `AGENTS.md` 与 `kuno-workflow-onboard-skills/`。命中项均为既有 Trellis / Channel / workflow 规则；本次没有 accepted-channel Trellis 变化，因此不修改 AGENTS 或 Skill 模板。

## GitNexus

### v1.6.7 -> v1.6.7

检测结论：`ENTRYPOINT.md` 中 GitNexus 当前版本为 `v1.6.7`，版本通道策略为 `stable-only`。GitHub Releases 列表中最新可见版本为 `v1.6.8-rc.35`，标记为 prerelease；release body 明确说明 release candidates 是 pre-stable builds，稳定版本仍在 `latest` dist-tag。npm `gitnexus` 的 `latest` dist-tag 仍为 `1.6.7`，`rc` dist-tag 为 `1.6.8-rc.35`。因此本次 accepted-channel 终点仍为 `v1.6.7`。

来源依据：

- GitHub Releases API：可见 `v1.6.8-rc.35`，`prerelease: true`，发布日期为 2026-06-14；release body 标注稳定版仍使用 `latest` dist-tag。
- GitHub release-by-tag / latest API：本次请求 `v1.6.7` tag 和 latest 端点返回 504，未作为唯一依据。
- npm metadata：`gitnexus` 的 `latest` 为 `1.6.7`，`rc` 为 `1.6.8-rc.35`。

Release 汇总：本次从 `v1.6.7` 到 `v1.6.7` 没有新的稳定版 release 需要汇总。`v1.6.8-rc.*` 系列包含 embeddings VECTOR index 相关修复等 rc 变更，但按 stable-only 策略只记录为 prerelease 观察项。

破坏性变更：无新的稳定版破坏性变更。

迁移说明：无需迁移；每日自动化不应把 rc 写回 `ENTRYPOINT.md`。如用户需要试用 rc，应作为单独手动决策处理。

对 agent harness workflow 的影响分析：已用 `1.6.8-rc`、`VECTOR`、`vector`、`embeddings`、`branch-scoped`、`control-flow`、`taint`、`Route node`、`gitnexus` 等关键词扫描 `AGENTS.md` 与 `kuno-workflow-onboard-skills/`。命中项均为既有 GitNexus 安装、MCP 可用性、索引判断和分析边界说明；本次没有 stable-channel 新能力、命令、配置或兼容性变更，因此不修改 AGENTS 或 Skill 模板。
