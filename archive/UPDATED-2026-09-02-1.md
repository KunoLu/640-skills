# UPDATE

## Codex v0.151.0 -> v0.152.0

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v0.151.0`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `rust-v0.152.0`（2026-09-01T01:58:32Z，`prerelease=false`，`draft=false`）。npm `@openai/codex` 的 `dist-tags.latest=0.152.0`。其后仅有 `rust-v0.153.0-alpha.*`（最新见到 `rust-v0.153.0-alpha.2`）以及 npm `alpha=0.153.0-alpha.2`；通道策略为 `stable-only`，不把 alpha 当作目标版本。本区间比较 `v0.151.0` 到 `v0.152.0`。

### 证据来源

- 有依据：
  - `https://api.github.com/repos/openai/codex/releases/tags/rust-v0.152.0`：`prerelease=false`，`draft=false`，发布时间 `2026-09-01T01:58:32Z`。
  - npm `@openai/codex`：`dist-tags.latest=0.152.0`，`alpha=0.153.0-alpha.2`。
  - 目标 tag Release body：Vim 搜索、rate-limit banner、MCP package-style 名称、`output_token_limit`、planning tool 默认关闭、MCP 缓存刷新后仍可用、subagent 跟随根 service tier 等。
  - GitHub compare `rust-v0.151.0...rust-v0.152.0`：`ahead_by=90`，`behind_by=4`，`total_commits=90`，API `file_count=300`。反向 compare 的 4 个 commit 是 `v0.151.0` 上的 backport / release 提交（含 `#41380`、`#41393` 与 release 标题 commit），对应能力已出现在 `v0.152.0` Release body，不构成目标版本缺功能。
  - 目标 tag `codex-rs/config/src/config_toml.rs`：`UpdatePlanToolConfig.enabled` 为 `#[serde(default)]`（`bool` 默认 `false`）。对比 `rust-v0.151.0` 同结构为 `#[serde(default = "default_true")]`。
  - 目标 tag `codex-rs/cli/src/mcp_cmd.rs` `validate_server_name`：允许 ASCII 字母数字以及 `-`、`_`、`:`、`@`、`/`、`.`。对比 `rust-v0.151.0` 仅允许字母数字、`-`、`_`。
  - 目标 tag `codex-rs/cli/tests/mcp_add_remove.rs`：`codex mcp add` 接受 `npm:@modelcontextprotocol/server-sequential.thinking`。
  - 目标 tag `codex-rs/config/src/mcp_types.rs`：`McpServerToolConfig.output_token_limit` 为可选正整数；`mcp_types_tests.rs` 拒绝 `0` / 负数。
  - 目标 tag `codex-rs/core/tests/suite/subagent_service_tier.rs`：`v0.151.0` 为 404，`v0.152.0` 存在。根 `service_tier` 变更会作用到已有 / 后续 child 请求，但“不得改写 child-owned settings”。
  - 目标 tag hook schema：`interrupt` / `stop` / `subagent-stop` 的 generated JSON 与 `v0.151.0` blob SHA 相同；`interrupt_hooks.rs` 测试文件 SHA 也相同。
  - 目标 tag `codex-rs/core-plugins/src/executor_hooks.rs`：`v0.151.0` 只给 bundled browser 配 `HookEventName::Stop`；`v0.152.0` 增加 `Interrupt` 与 `SubagentStop`。`hook_config.rs` 仍是 12 个 `HookEventName`。
  - 目标 tag 无 `.trellis/workflow.md`、无根 `workflow.md`。`HookMigration.ts` / `SubagentMigration.ts` 的 blob SHA 与 `v0.151.0` 相同，不在本区间 compare 文件列表中。
- 缺失：
  - 目标 tag `docs/config.md` 仍是指向 developers.openai.com 的短 stub，未展开 `tools.update_plan`、MCP 名称字符集或 `output_token_limit`；用户面向配置键以 tagged 源码、CLI 与 Release body 为准。
  - compare API 文件列表封顶 300，未覆盖全部 90 commits 的逐文件 diff；用户面向默认值与兼容性以 Release body 与已取得的 tagged 源为准。
  - Trellis 式 workflow 模板：目标 tag 不存在 `.trellis/workflow.md` 或根 `workflow.md`。Codex 仓库内 `.github/workflows/*` 是 CI，不是 Trellis workflow gate。
  - Trellis 式 upgrade / migration manifest：目标 tag 不存在。Codex 自有 `HookMigration.ts` / `SubagentMigration.ts` / `external-agent-migration` 与 SQLite `state/migrations` 是 Codex 产品内部迁移，且本区间相关 protocol 文件 SHA 未变；不得当作 Trellis 平台调度或 hook schema 变更依据。
  - 本仓库不是 Trellis 项目，不存在生成的 `.codex/**` 或 `.omp/**`；不得用未发布 `main` 或当前 OMP host 推断调度规则。
  - 未读取 `v0.153.0-alpha.*` 的 body / compare；alpha 不能作为规则依据。

### 发布汇总

`v0.151.0 -> v0.152.0`（依据 Release body 与 tagged 源）：

- 默认值变化：planning / `update_plan` 工具默认关闭；需 `tools.update_plan.enabled = true` 才启用（`#41744`）。tagged `UpdatePlanToolConfig.enabled` 从 `default_true` 改为 `bool` 默认 `false`。
- 功能首次引入 / 既有校验放宽：MCP 服务器名可含 `:`、`@`、`/`、`.`，支持 package-style 名称（`#41700`）。CLI `validate_server_name` 与 `mcp add` 测试覆盖 `npm:@...` 形式。
- 功能首次引入：单个 MCP tool 可配置 `output_token_limit`，恢复会话时截断语义一致（`#41421`）。
- 功能首次引入：app-server 客户端可配置 `thread/shellCommand` 超时，包括超过一小时（`#41384`）。
- 既有能力修复：MCP 工具在缓存刷新和 remote plugin 变更后仍可用；认证重试使用刷新后的 helper header（`#41336`、`#41344`、`#41396`、`#41400`）。
- 既有能力修复：subagent 跟随根 service tier（`#41308`）。tagged 测试证明根路由策略更新 child 请求的 `service_tier`，但不改写 child-owned settings。这是 Codex runtime 配额，不是 Trellis dispatch。
- 既有能力修复：中断 turn 仍跑 executor hook（`#41432`）；bundled browser cleanup 从仅 `Stop` 扩展到 `Interrupt` / `SubagentStop`（`#41435`）。hook 事件 schema JSON 未变，不是新 hook 名。
- 既有能力修复：Guardian 审批可在 compaction 后保留用户指令、回答和有效授权（`#41660`、`#41846`、`#41852`）。
- TUI / exec：Vim `/` `?` 搜索、rate-limit banner、凭证刷新进度、Windows sandbox / JediTerm 修复。不进入本仓库长期规则。
- 安全：cloud task 拒绝不受信 backend URL 并禁用 redirect（`#41403`）。不写入本仓库模板。

### 破坏性变更 / 迁移

- Release body 未列出强制迁移步骤。
- 从 `v0.151.0` 升到 `v0.152.0` 后，未显式开启时 `update_plan` 不再出现在工具列表；不要把缺失当成安装失败，也不要静默写回 `tools.update_plan.enabled = true`。
- 既有仅含字母数字 / `-` / `_` 的 MCP 名称继续有效。新的 package-style 名称合法，不要改写成连字符名。
- 本仓库模板不写入 `tools.update_plan.enabled`、`output_token_limit` 或 MCP 名称字符集白名单；不要把这些新能力静默写进用户 MCP / Codex 配置。

### 平台调度核验

Release body 含 `#41308`（subagent）与 `#41432` / `#41435`（hook），已按第 6.1 步核验目标 tag，不得只凭 release body。

有效配置：
- `docs/config.md` 为 stub。
- `features.multi_agent_v2` 仍为 `Stage::Stable` / `default_enabled: false`。
- 源码 / schema 中没有 `codex.dispatch_mode`。

workflow 模板：
- 目标 tag 无 `.trellis/workflow.md`、无根 `workflow.md`。该项不存在。
- `.github/workflows/*` 仅为 CI。

migration / upgrade manifest：
- 目标 tag 无 Trellis 式 upgrade / migration manifest。该项不存在。
- `HookMigration.ts` / `SubagentMigration.ts` blob SHA 与 `v0.151.0` 相同，本区间 compare 未改这些文件。

分类：
- `#41308`：既有 Codex nested subagent 的 runtime 配额修复；新测试 `subagent_service_tier.rs` 证明根 `service_tier` 传到 child 请求，且不改写 child-owned settings。不是新的 dispatch 模式或第二写入执行者。
- `#41432`：既有 `HookEventName::Interrupt`（`interrupt_hooks.rs` SHA 未变）在中断 turn 上跑 executor hook。
- `#41435`：既有 bundled browser cleanup 从 `Stop` 扩展到 `Interrupt` / `SubagentStop`；`subagent-stop` / `interrupt` / `stop` generated schema SHA 未变。

平台集成：本仓库无生成的 `.codex/**` / `.omp/**`。不得用当前 OMP host 或未发布 `main` 形成调度规则。

处理决定：缺少 Trellis workflow 模板与 Trellis migration manifest，不得形成或更新平台调度规则。既有规则继续有效：`.trellis/**` 只定义共享 workflow gate；Codex `dispatch_mode` / Inline fallback 仅适用于 Codex host；OMP 使用生成的 `task` worker；Channel 保持显式确认。不得把 Codex subagent service tier 或 Interrupt / SubagentStop / Stop hook 泛化为 Trellis Channel 或 role subagent。

### 影响分析

扫描关键词：`update_plan`、`tools.update_plan.enabled`、`output_token_limit`、`package-style`、`validate_server_name`、`mcp add`、`dispatch_mode`、`codex.dispatch_mode`、`SubagentStart`、Channel、`Guardian`、`mcp_optional_startup_grace`。

命中结果：

- 允许扫描路径中未命中 `update_plan`、`output_token_limit`、`package-style`。
- `AGENTS.global.md` 已有 plugin catalog ≠ 已授权、optional MCP 启动宽限期、extension 可替换 MCP tool result，以及不得静默改写 MCP / plugin 配置；尚未写明 `update_plan` 默认关闭、package-style MCP 名称合法、不得静默写入 `output_token_limit`。
- `README.md` / `README.html` 工作流主线已写 catalog 只是候选信号与 optional MCP 宽限期；尚未写内置 planning 工具默认关闭与 package-style MCP 名称。
- `AGENTS.md`、版本检查 prompt、`AGENTS.project.md`、`trellis-workflow`、`trellis-channel` 命中的仍是既有 Trellis / Codex dispatch / hook / Channel 核验规则，不是本区间新的 dispatch 命令。
- `onboard.py` / `install.sh` / `install.ps1` 使用 `gitnexus` 等简单 MCP 名，不限制用户自定义名称字符集；无人值守自动化不得修改安装器与测试。
- `catalog.json`、`catalog.schema.json`、项目 `.gitignore`、`tests/**`：未锁定上述新命令名。

处理决定：最小化更新 `AGENTS.global.md`、`README.md`、`README.html` 和 `CHANGELOG.md` 未发布章节。不写回 `ENTRYPOINT.md`。不把 `v0.152.0` 或 `v0.153.0-alpha.*` 写进长期规则。

## Trellis v0.6.16 -> v0.6.16

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v0.6.16`。GitHub Releases API 对该仓库仍返回空列表，因此用 tags + npm 交叉确认。最新 stable tag 仍为 `v0.6.16`。更高 tag 仅有 `v0.7.0-beta.*`，最新见到 `v0.7.0-beta.3`；npm `@mindfoldhq/trellis` 的 `latest=0.6.16`，`beta=0.7.0-beta.3`。通道策略为 `stable-only`，不跳到 beta。本区间无新 stable。

### 证据来源

- 有依据：
  - Git tags：`v0.6.16` 存在（commit `88f4834449da9b4f607ec05e322408a0aa66f2ce`）；更高仅为 `v0.7.0-beta.0` 到 `v0.7.0-beta.3`。
  - npm：`@mindfoldhq/trellis` 的 `dist-tags.latest=0.6.16`，`beta=0.7.0-beta.3`。
- 缺失：
  - GitHub Release body：`/releases` 为空。
  - 本次无新 stable 区间，未再展开 docs changelog、compare、migration manifest 或平台集成。
  - 未读取 `v0.7.0-beta.*`；beta 不能作为规则依据。
  - 本仓库无 `.trellis/`，也没有生成的 `.codex/**` / `.omp/**`。

### 破坏性变更 / 迁移

无。无需迁移。

### 平台调度核验

无新 tagged stable 证据。不形成或更新平台调度规则。

### 影响分析

本地已有 `task.py rename`、`trellis ablate` / `restore`、空 jsonl 启动门禁、`task_error`、OMP `skip_keyword` 与 Codex/OMP 调度边界。无新 stable 关键词需要改规则。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。

## GitNexus v1.6.10 -> v1.6.10

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v1.6.10`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `v1.6.10`（2026-08-27T22:49:55Z）。npm `gitnexus` 的 `dist-tags.latest=1.6.10`，`rc=1.6.11-rc.32`。其后只有 `v1.6.11-rc.*`；通道策略为 `stable-only`，不把 RC 当作目标版本。本区间无新 stable。

### 证据来源

- 有依据：
  - GitHub Releases 首页最新非 prerelease 为 `v1.6.10`。
  - `https://api.github.com/repos/abhigyanpatwari/GitNexus/releases/latest`：`tag=v1.6.10`，`prerelease=false`，`draft=false`。
  - npm `gitnexus`：`dist-tags.latest=1.6.10`，`rc=1.6.11-rc.32`。
- 缺失：
  - 本次无新 stable 区间，未再展开 docs changelog、compare 或 migration manifest。
  - 未读取 `v1.6.11-rc.*`；RC 不能作为规则依据。

### 破坏性变更 / 迁移

无。无需迁移。

### 平台调度核验

无新 tagged stable 证据。不形成或更新平台调度规则。不得把 GitNexus Codex plugin 写成 Trellis role subagent 或 OMP worker。

### 影响分析

扫描关键词：`bm25_threshold`、`embedding_threshold`、`skip-embeddings`、`skipEmbeddings`、`self-commit`、`repository allowlist`、`fail-closed`、`22.18`。

命中结果：本地已有升级后必须 CLI 重新 analyze、Node `engines` 下限、MCP allowlist / fail-closed 只读、已移除 matching 旋钮、`--self-commit` 与 plugin marketplace 不能替代全局 `gitnexus-mcp`。无新 stable 关键词需要改规则。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。

## Playwright v1.62.1 -> v1.62.1

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v1.62.1`。GitHub 最新 stable 仍是 `v1.62.1`（2026-07-30T16:35:55Z）。npm `playwright` 与 `@playwright/test` 均为 `latest=1.62.1`。更高仅有 `next=1.63.0-alpha-2026-09-01`；`stable-only` 忽略 alpha。

### 证据来源

- 有依据：
  - GitHub `/releases/latest`：`tag=v1.62.1`，`prerelease=false`，`draft=false`。
  - npm `playwright` / `@playwright/test`：`latest=1.62.1`。
- 缺失：本次无新 stable 区间。

### 破坏性变更 / 迁移

无。无需迁移。

### 影响分析

本地 Playwright CLI / MCP / 报告路径规则已覆盖当前 stable。无新版本关键词需要改规则。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。

## Maestro cli-2.10.0 -> cli-2.10.0

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `cli-2.10.0`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `cli-2.10.0`（2026-08-31T17:15:09Z）。其后没有更新的 stable CLI tag；更早的 `cli-2.3.1-dev.*` 为 prerelease，忽略。

### 证据来源

- 有依据：
  - GitHub Releases 首页最新 stable 为 `cli-2.10.0`。
- 缺失：本次无新 stable 区间，未再展开 changelog。

### 破坏性变更 / 迁移

无。无需迁移。

### 影响分析

本地 Maestro Java 17+ / CLI / MCP / 报告路径规则已覆盖当前 stable。无新版本关键词需要改规则。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。
