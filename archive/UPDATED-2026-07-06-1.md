# UPDATE

## Codex

### v0.142.5 -> v0.142.5

- 版本结论：`ENTRYPOINT.md` 中 Codex 当前使用版本为 `v0.142.5`；GitHub Releases 最新稳定版仍是 `rust-v0.142.5`，npm `@openai/codex` 的 `latest` dist-tag 仍是 `0.142.5`。GitHub tag 中的 `rust-` 前缀按本仓库惯例规范化为 `v0.142.5`。
- 本轮可见的新版本信号：GitHub Releases 和 npm `alpha` dist-tag 显示 `0.143.0-alpha.36`，属于 prerelease；当前策略为 `stable-only`，因此本轮不比较该 alpha 通道，也不把它写回 `ENTRYPOINT.md`。
- release 汇总：稳定通道没有新增版本。当前稳定 release 仍是 `0.142.5`，其已知内容是修复 Responses WebSocket 请求 payload 被写入 trace logs 的问题；这是当前基线版本本身的内容，不构成本轮新增区间。
- 破坏性变更：本轮稳定通道无新增破坏性变更。
- 迁移说明：不需要迁移；不需要执行 Codex 配置或 workflow 更新。
- 对 agent harness workflow 的影响分析：本地扫描命中了既有 `trace`、日志、敏感数据和报告安全规则；当前规则已经覆盖“不要把请求 payload、密钥、PII、生产数据写入日志、trace、截图或报告”的边界。本轮只观察到被 stable-only 忽略的 alpha release，不修改 `AGENTS.md`、agent 模板、onboard Skill 或 Skill 模板。
- 依据来源：[GitHub latest release](https://github.com/openai/codex/releases/tag/rust-v0.142.5)、[GitHub alpha release](https://github.com/openai/codex/releases/tag/rust-v0.143.0-alpha.36)、npm `@openai/codex` metadata。

## Trellis

### v0.6.5 -> v0.6.5

- 版本结论：`ENTRYPOINT.md` 中 Trellis 当前使用版本为 `v0.6.5`；GitHub Tags 最新稳定 tag 仍是 `v0.6.5`，npm `@mindfoldhq/trellis` 的 `latest` dist-tag 仍是 `0.6.5`。
- release 信息完整性：GitHub Releases API 当前返回空列表，未提供 release body；本轮用 GitHub Tags 与 npm metadata 交叉确认稳定通道仍停留在 `0.6.5`。npm 的 `beta` / `rc` dist-tags 分别停留在 prerelease 通道，因策略为 `stable-only` 被忽略。
- release 汇总：稳定通道没有新增版本；没有新的官方 release notes 需要汇总。
- 破坏性变更：本轮稳定通道无新增破坏性变更。
- 迁移说明：不需要执行 `trellis update` 或修改 `.trellis` 相关规则；本仓库也没有 `.trellis/` 或 `.trellis/workflow.md`，所以 Trellis 阶段跳过。
- 对 agent harness workflow 的影响分析：当前没有新的稳定 release-note 依据触发 Trellis workflow、Channel、spec template、migration manifest 或 onboard 规则变更；不修改 `AGENTS.md`、agent 模板、onboard Skill 或 Skill 模板。
- 依据来源：[GitHub tags](https://github.com/mindfold-ai/trellis/tags)、npm `@mindfoldhq/trellis` metadata。

## GitNexus

### v1.6.9 -> v1.6.9

- 版本结论：`ENTRYPOINT.md` 中 GitNexus 当前使用版本为 `v1.6.9`；GitHub Releases 最新稳定版为 `v1.6.9`，npm `gitnexus` 的 `latest` dist-tag 仍是 `1.6.9`。
- 本轮可见的新版本信号：GitHub Releases 和 npm `rc` dist-tag 显示 `v1.6.10-rc.3`，属于 prerelease；当前策略为 `stable-only`，因此本轮不比较该 rc 通道，也不把它写回 `ENTRYPOINT.md`。
- release 汇总：稳定通道没有新增版本。当前基线 `v1.6.9` 的 release notes 包含 cross-repo call trace over PDG、HTTP route / consumer extraction、Java/Python taint source/sink models、默认 workspace index 跟随当前 checkout branch、FTS / embedding 搜索改进等；这些已经属于当前基线版本，不构成本轮新增区间。
- 破坏性变更：本轮稳定通道无新增破坏性变更。
- 迁移说明：不需要重新分析或更新 GitNexus 索引。当前仓库没有 `.gitnexus/`，因此 GitNexus MCP / index 检查跳过。
- 对 agent harness workflow 的影响分析：本地扫描命中了既有 `.gitnexus/gitnexus.json` / `.gitnexus/meta.json`、stale index、PDG / taint / trace、MCP transport、hook 与跨服务路由复核规则；当前规则已覆盖 `v1.6.9` 的稳定功能边界。`v1.6.10-rc.3` 中的 CodeBuddy、Qoder、Codex setup、plugin marketplace 和 proxy-blocked install 只属于 prerelease 观察，不触发长期 AGENTS 或 Skill 规则修改。
- 依据来源：[GitHub v1.6.9 release](https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.9)、[GitHub v1.6.10-rc.3 release](https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.10-rc.3)、npm `gitnexus` metadata。

## Playwright

### v1.61.1 -> v1.61.1

- 版本结论：`ENTRYPOINT.md` 中 Playwright 当前使用版本为 `v1.61.1`；GitHub Releases 最新稳定版仍是 `v1.61.1`，npm `@playwright/test` 与 `playwright` 的 `latest` dist-tag 仍是 `1.61.1`。
- 本轮可见的新版本信号：npm `next` dist-tag 显示 `1.62.0-alpha-2026-07-05`，属于 prerelease / next 通道；当前策略为 `stable-only`，因此本轮不比较该通道，也不写回 `ENTRYPOINT.md`。
- release 汇总：稳定通道没有新增版本。当前基线 `v1.61.1` 的 release notes 是 bug fix patch，涉及 custom matcher 覆盖、UI mode API request 字节数、trace viewer websocket 时间缩放、Node 22.15 sync loader、pnpm workspace symlink 下 extensionless `.ts` 子路径解析等；这些已经属于当前基线版本，不构成本轮新增区间。
- 破坏性变更：本轮稳定通道无新增破坏性变更。
- 迁移说明：不需要更新 Playwright reporter、配置或测试资产路径。
- 对 agent harness workflow 的影响分析：本地现有规则已经区分 Playwright CLI、Playwright MCP、正式 HTML report、runner 临时目录、trace / video / screenshot 与 Markdown 汇总 stem；本轮没有新的稳定 release-note 依据要求修改 Web E2E 或报告 gate。
- 依据来源：[GitHub v1.61.1 release](https://github.com/microsoft/playwright/releases/tag/v1.61.1)、npm `@playwright/test` metadata、npm `playwright` metadata。

## Maestro

### cli-2.6.1 -> cli-2.6.1

- 版本结论：`ENTRYPOINT.md` 中 Maestro 当前使用版本为 `cli-2.6.1`；GitHub Releases 最新稳定 CLI release 仍是 `cli-2.6.1`，GitHub Tags 也显示 `v2.6.1` / `cli-2.6.1` 为当前稳定线索。
- release 信息完整性：GitHub release body 指向 `CHANGELOG.md`，raw changelog 的 `Unreleased` 当前为空；`2.6.1` 条目包含 iOS cross-process sheets 与 hierarchy walk 性能修复、`fetchCrossOriginIframeContent` stale iframe 修复，以及 CLI 提示 URL 更新。
- release 汇总：稳定通道没有新增版本；没有新的 Maestro CLI release 需要汇总。
- 破坏性变更：本轮稳定通道无新增破坏性变更。
- 迁移说明：不需要更新 Java / Maestro CLI / Maestro MCP 检测顺序、flow 资产路径或报告命名规则。
- 对 agent harness workflow 的影响分析：本地现有规则已经覆盖 Java 17+、Maestro CLI、Maestro MCP、view hierarchy、正式 report / Markdown 汇总和 iOS 真机排障 lesson 的触发条件；本轮没有新的稳定 release-note 依据要求修改 agent harness workflow。
- 依据来源：[GitHub cli-2.6.1 release](https://github.com/mobile-dev-inc/Maestro/releases/tag/cli-2.6.1)、[Maestro CHANGELOG.md](https://github.com/mobile-dev-inc/Maestro/blob/main/CHANGELOG.md#261)。
