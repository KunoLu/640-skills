# UPDATE

## Codex

### v0.142.5 -> v0.142.5

本次检查未发现高于 `ENTRYPOINT.md` 基线 `v0.142.5` 的稳定版本。GitHub Releases 当前将 `0.142.5` 标记为 Latest；同页可见 `0.143.0-alpha.32` 为 Pre-release，按 `stable-only` 策略不纳入本次比较。Tags 页也显示最新前置 tag 为 `rust-v0.143.0-alpha.32`，最新稳定 tag 为 `rust-v0.142.5`。

- 版本来源：GitHub Releases（`https://github.com/openai/codex/releases`）、GitHub Tags（`https://github.com/openai/codex/tags`）。
- release 汇总：比较区间内没有新增稳定 release。当前基线版本 `0.142.5` 的 release note 本身包含一次 bug fix：避免完整 Responses WebSocket 请求 payload 写入 trace logs；由于该版本已经是 `ENTRYPOINT.md` 基线，本次不把它当作新增升级内容推进。
- 破坏性变更：未发现新增稳定版本，因此没有新的破坏性变更。
- 迁移说明：无。
- 对 agent harness workflow 的影响分析：本地规则已包含不要把真实账号、密钥、PII、生产数据、完整 token、敏感请求头写入日志、截图、trace、video 或报告的通用约束；本次没有更高稳定版本，不需要修改 `AGENTS.md` 或 Skill 模板。
- 本地规则命中扫描：`trace`、`token`、`PII`、敏感信息相关规则已命中 `kuno-workflow-onboard-skills/templates/agents/AGENTS.global.md`、`AGENTS.project.md`、`project-validation`、`maestro-mobile-e2e` 等文件。处理决定：保留现状。

## Trellis

### v0.6.5 -> v0.6.5

本次检查未发现高于 `ENTRYPOINT.md` 基线 `v0.6.5` 的可比较稳定版本。GitHub Releases 页面显示该仓库没有 releases；GitHub Tags 页当前可见的最新稳定 tag 为 `v0.6.0`，并有 `v0.6.0-rc.0`、`v0.6.0-beta.*` 等预发布 tag，但没有可见的 `v0.6.5` tag 或更高稳定 tag。

- 版本来源：GitHub Releases（`https://github.com/mindfold-ai/trellis/releases`）、GitHub Tags（`https://github.com/mindfold-ai/trellis/tags`）。
- release 汇总：GitHub Releases 缺失，tags 中也未发现高于 `v0.6.5` 的稳定版本；因此没有可汇总的新增 release notes。
- 破坏性变更：无可追溯新增稳定 release，因此未发现新的破坏性变更。
- 迁移说明：无。
- 对 agent harness workflow 的影响分析：当前仓库基线 `v0.6.5` 高于 GitHub Tags 当前可见稳定 tag `v0.6.0`，这属于来源可追溯性风险，不适合作为自动写回或规则修改依据。继续按现有 Trellis 启用条件、workflow gate、Channel 边界和 `.trellis/` 目录规则执行。
- 缺失来源说明：缺少 GitHub release body、`v0.6.5` tag、可比较的 release notes、migration manifest 或 GitHub compare 区间。处理决定：记录风险，不修改长期规则，不修改 `ENTRYPOINT.md`。

## GitNexus

### v1.6.8 -> v1.6.8

本次检查未发现高于 `ENTRYPOINT.md` 基线 `v1.6.8` 的稳定版本。GitHub Releases 当前第一页有 `v1.6.9-rc.*` release candidate，release body 明确说明 release candidates 是 pre-stable build，stable releases 保持在 `latest` dist-tag；`v1.6.8` 仍标记为 Latest。按 `stable-only` 策略，本次不把 `v1.6.9-rc.*` 作为升级目标。

- 版本来源：GitHub Releases（`https://github.com/abhigyanpatwari/GitNexus/releases`）、GitHub Tags（`https://github.com/abhigyanpatwari/GitNexus/tags`）。
- release 汇总：比较区间内没有新增稳定 release。已跳过的 `v1.6.9-rc.*` 预发布内容涉及 taint source/sink model、Kotlin / Django route extraction、PDG cross-repo call trace、MCP impact callgraph 容错等，但这些仍属于 RC 通道。
- 破坏性变更：未发现新增稳定版本，因此没有新的稳定版破坏性变更。
- 迁移说明：无。
- 对 agent harness workflow 的影响分析：现有全局规则已覆盖 PDG / taint / trace 仅作为显式 opt-in、Streamable HTTP / legacy SSE MCP transport 不静默改配置、跨服务 API / HTTP route / consumer 结论必须回到源码和 diff 交叉核对等边界。RC 内容不触发长期规则更新。
- 本地规则命中扫描：`PDG`、`taint`、`trace`、`Streamable HTTP`、`legacy SSE`、`HTTP route`、`consumer` 等关键词已命中 `kuno-workflow-onboard-skills/templates/agents/AGENTS.global.md`、`AGENTS.project.md` 和相关 Skill。处理决定：保留现状。

## Playwright

### v1.61.1 -> v1.61.1

本次检查未发现高于 `ENTRYPOINT.md` 基线 `v1.61.1` 的稳定版本。GitHub Releases 当前将 `v1.61.1` 标记为 Latest；release 列表中更早版本包括 `v1.61.0`、`v1.60.0` 等。

- 版本来源：GitHub Releases（`https://github.com/microsoft/playwright/releases`）、GitHub Tags（`https://github.com/microsoft/playwright/tags`）。
- release 汇总：比较区间内没有新增稳定 release。当前基线版本 `v1.61.1` 的 release note 主要是 bug fixes，包括 `expect.extend` matcher 覆盖、UI mode API request byte 统计、trace viewer WebSocket 时间缩放、Node 22 sync loader 和 pnpm workspace symlink 下 extensionless `.ts` subpath imports 回归修复；由于该版本已经是 `ENTRYPOINT.md` 基线，本次不作为新增升级内容推进。
- 破坏性变更：未发现新增稳定版本，因此没有新的破坏性变更。
- 迁移说明：无。
- 对 agent harness workflow 的影响分析：现有 Playwright 规则已经聚焦 CLI 与 MCP 职责区分、项目内安装确认、HTML report / trace / Markdown 汇总路径、失败重跑顺序和敏感信息保护。本次没有更高稳定版本，不需要修改规则或 Skill。
- 本地规则命中扫描：`Trace viewer`、`Web Storage`、`passkey`、`Node 22` 等本次相关关键词没有命中需要新增长期约束的本地模板；`trace` / report 相关通用规则已覆盖。处理决定：保留现状。

## Maestro

### cli-2.6.1 -> cli-2.6.1

本次检查未发现高于 `ENTRYPOINT.md` 基线 `cli-2.6.1` 的 CLI 稳定版本。GitHub Releases 当前最新为 `CLI 2.6.1`，Tags 页同时可见 `v2.6.1` 和 `cli-2.6.1`，二者指向同一发布时间和提交。

- 版本来源：GitHub Releases（`https://github.com/mobile-dev-inc/Maestro/releases`）、GitHub Tags（`https://github.com/mobile-dev-inc/Maestro/tags`）。
- release 汇总：比较区间内没有新增稳定 release。`CLI 2.6.1` release body 指向仓库 `CHANGELOG.md`，但由于当前基线已经是该版本，本次没有需要从当前版本向后汇总的新增变更。
- 破坏性变更：未发现新增稳定版本，因此没有新的破坏性变更。
- 迁移说明：无。
- 对 agent harness workflow 的影响分析：现有规则已经覆盖 Java 17+ gate、Maestro CLI / MCP 职责、Mobile / Hybrid flow 资产路径、`.maestro/reports/` 正式报告、iOS 真机 driver / transport / view hierarchy / tap crash 排障 lesson 的懒加载边界。本次不需要修改 AGENTS 或 Skill 模板。
- 本地规则命中扫描：`Maestro`、`Java 17`、`driver`、`view hierarchy`、`cli-2.6.1` 等关键词已命中全局 / 项目 AGENTS、`project-validation`、`maestro-mobile-e2e` 及其 references。处理决定：保留现状。
