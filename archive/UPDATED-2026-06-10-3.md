# UPDATE

## Codex

### v0.139.0 -> v0.139.0

**版本结论：** 本次以 `ENTRYPOINT.md` 中的 `v0.139.0` 作为固定起点。GitHub Releases 最新稳定版仍为 `rust-v0.139.0`，npm `@openai/codex` 的 `latest` dist-tag 仍为 `0.139.0`。npm / GitHub 上可见的 `0.140.0-alpha.2` 属于预发布通道，按 `stable-only` 策略忽略。

**依据来源：**

- GitHub Releases API：`https://api.github.com/repos/openai/codex/releases?per_page=10`
- GitHub Release 页面：`https://github.com/openai/codex/releases/tag/rust-v0.139.0`
- npm dist-tags：`rtk npm view @openai/codex dist-tags --json`

**Release 汇总：** 没有发现高于 `v0.139.0` 的新稳定版，因此本次没有从 `ENTRYPOINT.md` 当前版本到更高稳定版本的 release notes 需要汇总。GitHub API 中当前稳定 release body 为空；由于区间终点与起点相同，本次不据此推断新的工作流变化。

**破坏性变更：** 未发现本次可接受稳定区间内的新破坏性变更。

**迁移说明：** 不需要迁移；不要把 `0.140.0-alpha.2` 写回 `ENTRYPOINT.md`。

**对 agent harness workflow 的影响分析：** 当前仓库已有的 Codex / sandbox / rtk 命令使用规则仍适用。本次没有 release-note-backed 的长期规则变化，因此不修改 `AGENTS.md`、有效 agents 模板或 Skill 模板。

## Trellis

### v0.6.0-beta.23 -> v0.6.0-beta.23

**版本结论：** 本次以 `ENTRYPOINT.md` 中的 `v0.6.0-beta.23` 作为固定起点。GitHub Releases API 当前为空；GitHub Tags 中最新同 beta 通道仍为 `v0.6.0-beta.23`。可见 `v0.6.0-rc.0` 属于 rc 通道，按 `same-prerelease-channel` 策略不从 beta 主动跳转。npm `@mindfoldhq/trellis` 的 `beta` dist-tag 仍为 `0.6.0-beta.23`。

**依据来源：**

- GitHub Releases API：`https://api.github.com/repos/mindfold-ai/trellis/releases?per_page=10`
- GitHub Tags API：`https://api.github.com/repos/mindfold-ai/trellis/tags?per_page=30`
- GitHub tag 页面：`https://github.com/mindfold-ai/Trellis/releases/tag/v0.6.0-beta.23`
- npm dist-tags：`rtk npm view @mindfoldhq/trellis dist-tags --json`

**Release 汇总：** 没有发现高于 `v0.6.0-beta.23` 的同 beta 通道版本。由于本次区间没有跨版本变化，GitHub release body 缺失不会被写成“无可追溯变更”；本次依据 tags 与 npm dist-tags 确认可接受通道未推进，rc 通道仅记录为已过滤信息。

**破坏性变更：** 未发现本次同 beta 通道内的新破坏性变更。

**迁移说明：** 不需要运行新的迁移；已有 `trellis update`、registry-backed spec template、Channel runtime agent definition 等长期规则保持不变。不要把 `v0.6.0-rc.0` 写回 `ENTRYPOINT.md`。

**对 agent harness workflow 的影响分析：** 当前仓库没有 `.trellis/` 或 `.trellis/workflow.md`，因此 Trellis 阶段跳过。本次没有 release-note-backed 的新增长期规则，不修改 Trellis 相关 Skill 模板或 agents 模板。

## GitNexus

### v1.6.7 -> v1.6.7

**版本结论：** 本次以 `ENTRYPOINT.md` 中的 `v1.6.7` 作为固定起点。GitHub Releases 中最新稳定版仍为 `v1.6.7`，npm `gitnexus` 的 `latest` dist-tag 仍为 `1.6.7`。GitHub / npm 上可见 `v1.6.8-rc.7`，但它是 release candidate，按 `stable-only` 策略忽略。

**依据来源：**

- GitHub Releases API：`https://api.github.com/repos/abhigyanpatwari/GitNexus/releases?per_page=10`
- GitHub Release 页面：`https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.7`
- npm dist-tags：`rtk npm view gitnexus dist-tags --json`

**Release 汇总：** 没有发现高于 `v1.6.7` 的新稳定版，因此本次没有从 `v1.6.7` 到更高稳定版本的 release notes 需要汇总。`v1.6.8-rc.7` 中的 multi-branch indexing、Java Spring route extraction、impact / route_map 修复等内容仍属于预发布依据，本次只记录并过滤，不沉淀为长期规则。

**破坏性变更：** 未发现本次稳定区间内的新破坏性变更。

**迁移说明：** 不需要迁移；不要把 `v1.6.8-rc.7` 写回 `ENTRYPOINT.md`。

**对 agent harness workflow 的影响分析：** 当前仓库没有 `.gitnexus/` 索引证据，GitNexus MCP / index 阶段跳过。`v1.6.7` 相关的 `list_repos` 分页、`gitnexus uninstall` dry-run / `--force`、optional grammar / prebuild 风险规则已在有效 agents 模板中沉淀；本次无新稳定 release-note-backed 规则变化，因此不再修改 AGENTS 或 Skill 模板。
