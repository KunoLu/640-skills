# UPDATE

## Codex
### v0.142.5 -> v0.142.5

**检测结论**：`ENTRYPOINT.md` 当前基线为 `v0.142.5`，GitHub Releases 中最新稳定版仍是 `rust-v0.142.5`，npm `@openai/codex` 的 `latest` 仍是 `0.142.5`。`0.143.0-alpha` 系列属于 prerelease，按 `stable-only` 策略忽略，不推进本次区间终点。

**Release 汇总**：当前稳定版 `0.142.5` 是 bug fix release，主要修复 Responses WebSocket 请求完整 payload 被写入 trace logs 的问题。该内容已经落在现有敏感信息、日志、trace 和报告不得写入密钥 / PII / 生产数据的规则边界内。

**破坏性变更**：未发现新的稳定版破坏性变更。

**迁移说明**：不需要迁移；每日自动化不得写回 `ENTRYPOINT.md`。

**对 agent harness workflow 的影响分析**：本轮没有新的稳定版 workflow、命令、配置或兼容性变化。已用 `Responses`、`WebSocket`、`trace logs`、`payload`、`MCP` 等关键词扫描 `AGENTS.md`、`kuno-workflow-onboard-skills/` 下有效 agents 模板、Skill、`REFERENCE.md` 和 `onboard.py`；现有安全与报告规则已覆盖，不修改长期规则。

**依据来源**：

- GitHub release：`https://github.com/openai/codex/releases/tag/rust-v0.142.5`
- npm metadata：`@openai/codex` `latest=0.142.5`

## Trellis
### v0.6.5 -> v0.6.5

**检测结论**：`ENTRYPOINT.md` 当前基线为 `v0.6.5`，GitHub Tags 与 npm `@mindfoldhq/trellis` 的 `latest` 均确认最新稳定版仍是 `0.6.5`。`0.6.0-beta.23` 和 `0.6.0-rc.0` 属于 prerelease / RC 通道，按 `stable-only` 策略忽略。

**Release 汇总**：GitHub Releases 为空，无法直接从 release body 获取稳定版说明。已补充检查 GitHub compare、npm metadata、`packages/cli/package.json` 和 `packages/cli/src/migrations/manifests/0.6.5.json`。`0.6.5` 主要包含 Trae IDE 支持、Pi 启动上下文和 sub-agent 工具声明修复、Windows channel spawn 修复、空 stdin hook 修复、`implement.jsonl` / `check.jsonl` curated context gate，以及 brainstorm PRD lossless convergence 约束。

**破坏性变更**：migration manifest 标记 `breaking=false`，`recommendMigrate=false`，`migrations=[]`。

**迁移说明**：不需要 `--migrate`；上游建议升级后运行 `trellis update`。Trae 新接入项目可按需运行 `trellis init --trae`，既有项目仍优先 `trellis update`。

**对 agent harness workflow 的影响分析**：这些内容已经被本仓库现有 `trellis-workflow` 规则覆盖，包括 curated `implement.jsonl` / `check.jsonl`、pull-based sub-agent prelude、Pi startup context 和 `trellis update` 复核要求。本轮没有新的稳定版变化，不修改 `AGENTS.md`、agents 模板或 Skill。

**依据来源**：

- GitHub tags：`https://github.com/mindfold-ai/trellis/tags`
- GitHub compare：`https://github.com/mindfold-ai/trellis/compare/v0.6.4...v0.6.5`
- npm metadata：`@mindfoldhq/trellis` `latest=0.6.5`
- migration manifest：`https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.5/packages/cli/src/migrations/manifests/0.6.5.json`

## GitNexus
### v1.6.9 -> v1.6.9

**检测结论**：`ENTRYPOINT.md` 当前基线为 `v1.6.9`，GitHub Releases 与 npm `gitnexus` 的 `latest` 均确认最新稳定版仍是 `1.6.9`。`v1.6.10-rc.6` 是 prerelease release candidate，按 `stable-only` 策略忽略，不推进本次区间终点。

**Release 汇总**：当前稳定版 `v1.6.9` 已在基线中，主要能力包括跨仓 API trace、HTTP route / consumer 解析增强、Java / Python taint source / sink model、默认 workspace index 跟随当前 checkout branch、CJK FTS 分词、可配置 FTS stemmer / vector distance threshold，以及索引元数据文件改名为 `.gitnexus/gitnexus.json` 并保留 `.gitnexus/meta.json` 兼容镜像。

**破坏性变更**：未发现新的稳定版破坏性变更。`v1.6.10-rc.6` 中的 CodeBuddy / Qoder / full Codex setup、proxy-blocked install、onnxruntime、FTS load error 和 MCP line-display 修复仍是 RC 观察项，不沉淀为稳定规则。

**迁移说明**：无新的稳定版迁移动作。既有规则已要求检查 `.gitnexus/gitnexus.json` 与 `.gitnexus/meta.json`、stale index、multi-branch、PDG / taint / trace 和 MCP transport 边界。

**对 agent harness workflow 的影响分析**：本轮没有新的稳定版 workflow、命令或兼容性变化。已用 `gitnexus.json`、`meta.json`、`PDG`、`taint`、`HTTP route`、`CodeBuddy`、`Qoder`、`onnxruntime`、`FTS` 等关键词扫描本地规则；当前全局模板已经包含 metadata rename、stale index、分页、PDG / taint / trace、transport 与大仓库索引风险规则，不新增修改。

**依据来源**：

- GitHub release：`https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.9`
- npm metadata：`gitnexus` `latest=1.6.9`
- ignored RC：`https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.10-rc.6`

## Playwright
### v1.61.1 -> v1.61.1

**检测结论**：`ENTRYPOINT.md` 当前基线为 `v1.61.1`，GitHub Releases、GitHub Tags 与 npm `playwright` 的 `latest` 均确认最新稳定版仍是 `1.61.1`。npm `next=1.62.0-alpha-2026-07-06` 和 beta 构建属于非稳定通道，按 `stable-only` 策略忽略。

**Release 汇总**：当前稳定版 `v1.61.1` 是 bug fix release，修复 expect 扩展 matcher 覆盖默认 matcher、UI mode API request byte reporting、trace viewer WebSocket message timestamp 缩放、Node 22.15 sync loader 条件判断，以及 pnpm workspace symlink 下 extensionless TypeScript subpath import 解析问题。

**破坏性变更**：未发现新的稳定版破坏性变更。

**迁移说明**：不需要迁移。现有 Playwright CLI / MCP 分工、正式 HTML report 与同 stem 中文 Markdown 汇总规则不变。

**对 agent harness workflow 的影响分析**：本轮没有新的稳定版 workflow、命令、配置或 reporter 行为变化。已用 `Trace viewer`、`WebSocket`、`tsconfig`、`aria snapshot`、`Playwright` 等关键词扫描本地规则；当前报告路径、HTML reporter 临时目录、`results.json` / `junit.xml` 边界和 Playwright MCP 不替代 CLI 的规则仍适用，不修改长期规则。

**依据来源**：

- GitHub release：`https://github.com/microsoft/playwright/releases/tag/v1.61.1`
- npm metadata：`playwright` `latest=1.61.1`

## Maestro
### cli-2.6.1 -> cli-2.6.1

**检测结论**：`ENTRYPOINT.md` 当前基线为 `cli-2.6.1`，GitHub Releases 与 tags 均确认最新稳定 CLI 仍是 `cli-2.6.1` / `v2.6.1`。`CHANGELOG.md` 的 `Unreleased` 章节为空。

**Release 汇总**：当前稳定版 `2.6.1` 修复 iOS cross-process sheets，并改进 hierarchy walk 性能；修复 `fetchCrossOriginIframeContent` stale iframe 处理；CLI hints 从旧 URL 调整到 `app.maestro.dev`。

**破坏性变更**：未发现新的稳定版破坏性变更。

**迁移说明**：不需要迁移。Java 17+、Maestro CLI、Maestro MCP、JUnit / HTML report 和 `.maestro/reports/` 命名报告规则不变。

**对 agent harness workflow 的影响分析**：本轮没有新的稳定版 workflow、命令、配置或报告规则变化。已用 `cross-process`、`iframe`、`app.maestro.dev`、`Maestro Viewer`、`XCTest`、`JUnit`、`MCP` 等关键词扫描本地规则；当前 Maestro Java gate、CLI / MCP 分工、stdout-only 不满足正式报告 gate 的规则仍适用，不修改长期规则。

**依据来源**：

- GitHub release：`https://github.com/mobile-dev-inc/Maestro/releases/tag/cli-2.6.1`
- GitHub changelog：`https://raw.githubusercontent.com/mobile-dev-inc/Maestro/main/CHANGELOG.md`

