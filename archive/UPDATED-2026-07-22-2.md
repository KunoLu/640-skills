# UPDATE

本次检查以 `ENTRYPOINT.md` 的“当前使用版本”为固定起点；未写回任何版本基线。GitHub API 检查时间为 2026-07-23。

## Codex v0.145.0 -> v0.145.0

- **检测结果**：`openai/codex` 的最新 stable release 为 `rust-v0.145.0`；`rust-v0.146.0-alpha.2` 等 prerelease 按 `stable-only` 策略排除。`rust-` 前缀不改变与基线 `v0.145.0` 的语义版本比较结果。
- **release 汇总、破坏性变更与迁移**：没有跨版本更新，因此没有新增 release note、破坏性变更或迁移步骤需要应用。
- **证据来源**：GitHub Releases 有效；最新 stable release body 可读取，但不属于本次跨版本区间。未发现需要补充的 compare、migration manifest 或 npm 发布物证据。
- **agent harness workflow 影响分析**：无需修改本仓库规则。已按 Trellis 0.6.8 关键词扫描受允许的版本化规则、Onboard 文档、脚本、agents 模板与 bundled Skills；Codex 无跨版本更新，不产生可追溯规则变更。

## Trellis v0.6.7 -> v0.6.8

- **检测结果**：`mindfold-ai/trellis` 没有 GitHub Release；Tags API 的最新 stable tag 为 `v0.6.8`。npm registry 中 `@mindfoldhq/trellis` 的 `latest` 和 `0.6.8` 发布版本均为 `0.6.8`。
- **release 汇总**：官方 `packages/cli/src/migrations/manifests/0.6.8.json` 记录新增 Grok Build 与 Kimi Code 平台、Codex native subagent dispatch、`trellis platforms --json`、`task.py list/current --json`、Codex Channel `--sandbox` 覆盖和 `--base-branch` 覆盖；同时修复 registry 模板下载、SessionStart、frontmatter 引号、模板重新引入、原子写入与 task 边界等问题。
- **破坏性变更与迁移**：manifest 标记 `breaking: false`、`recommendMigrate: false`。但它声明 Pi 的共享 Skills 从 `.pi/skills` 迁到 `.agents/skills`，原因是 Pi 会同时发现两处目录而造成重复；上游说明该迁移可选但建议执行，既有 Pi 安装应通过 `trellis update --migrate` 处理，不能手工搬移或删除。
- **证据来源**：`/releases/tags/v0.6.8` 返回 404，故没有可用 release body。官方 migration manifest 与其 changelog 有效；GitHub Compare `v0.6.7...v0.6.8` 有效，报告 41 个提交和对应文件列表，包含平台、Channel、Codex 配置、update 与 migration 实现；npm metadata 有效。没有发现独立的同版本 GitHub Release 文本，未把其缺失误写为“无可追溯变更”。
- **agent harness workflow 影响分析**：已扫描 `AGENTS.md`、本 automation prompt、catalog/schema、Onboard 文档与脚本、全局/项目 agents 模板和全部 bundled Skills。仅 `trellis-workflow` 的既有迁移规则命中并不足以覆盖“manifest 不推荐、但 Pi 明确建议迁移”的情形，已最小化补充 legacy `.pi/skills` 检测与托管迁移规则。`SKILL.md`、`REFERENCE.md`、`onboard.py`、`AGENTS.project.md` 与 prompt 中的 `.agents/skills` 命中均为 React Bits、Kimi 或路径优先级语境，不适用此 Trellis 迁移；其余允许文件无相关命中，不修改。

## GitNexus v1.6.9 -> v1.6.9

- **检测结果**：`abhigyanpatwari/GitNexus` 的最新 stable release 为 `v1.6.9`；`v1.6.10-rc.*` 属 prerelease，按 `stable-only` 策略排除。
- **release 汇总、破坏性变更与迁移**：没有跨版本更新，因此没有新增 release note、破坏性变更或迁移步骤需要应用。
- **证据来源与影响**：GitHub Releases 有效；无需补充 compare、manifest 或 npm 证据。无规则修改。

## Playwright v1.61.1 -> v1.61.1

- **检测结果**：`microsoft/playwright` 的最新 stable release 为 `v1.61.1`，与基线一致。
- **release 汇总、破坏性变更与迁移**：没有跨版本更新，因此没有新增 release note、破坏性变更或迁移步骤需要应用。
- **证据来源与影响**：GitHub Releases 有效；无需补充 compare、manifest 或 npm 证据。无规则修改。

## Maestro cli-2.7.0 -> cli-2.7.0

- **检测结果**：`mobile-dev-inc/Maestro` 的最新 stable CLI release 为 `cli-2.7.0`，与基线一致。
- **release 汇总、破坏性变更与迁移**：没有跨版本更新，因此没有新增 release note、破坏性变更或迁移步骤需要应用。
- **证据来源与影响**：GitHub Releases 有效；无需补充 compare、manifest 或 npm 证据。无规则修改。
