# UPDATE

## Codex

### v0.144.0 -> v0.144.1

本次检测到 Codex 已发布新的 stable 版本。GitHub release `rust-v0.144.1` 于 2026-07-09 23:02:40 UTC 发布，npm `@openai/codex` 的 `latest` dist-tag 也指向 `0.144.1`，平台包 dist-tag 同步存在 `linux-x64`、`linux-arm64`、`darwin-x64`、`darwin-arm64`、`win32-x64` 和 `win32-arm64`。`0.145.0-alpha.1`、`0.145.0-alpha.2` 以及对应平台 alpha 包属于 prerelease，按 stable-only 策略忽略。

来源依据：

- GitHub release：<https://github.com/openai/codex/releases/tag/rust-v0.144.1>
- GitHub compare：<https://github.com/openai/codex/compare/rust-v0.144.0...rust-v0.144.1>
- npm metadata：<https://registry.npmjs.org/@openai/codex>

Release 汇总：

- 修复 standalone installer 在 GitHub 返回 compact 或字段重排的 release metadata 时解析失败的问题。
- macOS package install 现在会在 `codex` executable 旁暴露 code-mode host。
- 当 companion host binary 不可用时，code mode 会 fallback 到 embedded runtime，避免 code mode 因 host 缺失而不可用。
- GitHub compare 显示本区间包含 4 个 commit、8 个文件变更，主要集中在 `scripts/install/install.sh`、安装脚本测试，以及 `codex-rs/code-mode` / `core` 的 fallback 逻辑。

破坏性变更：

- Release notes 没有标记明确 breaking change。
- 影响面主要是 Codex 自身安装器与 code-mode 可用性，未发现需要用户迁移的 agent harness 配置变更。

迁移说明：

- 不需要在本仓库每日自动化中写回 `ENTRYPOINT.md`；版本基线仍保持 `v0.144.0`，只有用户手动输入 `更新` / `update` 时才写回。
- 如果用户遇到 Codex standalone installer 解析 release metadata、macOS code-mode host 或 companion host binary 缺失问题，应优先升级到本 stable 版本再复验。

对 agent harness workflow 的影响分析：

- 关键词扫描范围：`AGENTS.md`、`kuno-workflow-onboard-skills/SKILL.md`、`kuno-workflow-onboard-skills/REFERENCE.md`、`kuno-workflow-onboard-skills/scripts/onboard.py`、`kuno-workflow-onboard-skills/templates/agents/AGENTS.global.md`、`kuno-workflow-onboard-skills/templates/agents/AGENTS.project.md`、`kuno-workflow-onboard-skills/templates/skills/**`。
- 扫描关键词：`compact`、`reordered`、`release metadata`、`standalone install`、`code-mode`、`code mode`、`companion host`、`embedded runtime`、`install.sh`、`asset lookup`、`asset-digest`、`releases/latest`。
- 命中结果：本仓库只在 onboard Skill / reference / script 中命中通用 `install.sh`、安装 URL 和 GitHub releases API 文案；未命中 Codex standalone installer 的 release metadata 解析、code-mode host、companion host 或 embedded runtime 规则。
- 处理决定：本轮不修改 `AGENTS.md`、onboard script、AGENTS 模板或 Skill 模板。当前 release 只要求在 `UPDATE.md` 中记录 Codex 安装器与 code mode 修复，不构成可复用 agent harness workflow 规则变更。

## Trellis

### v0.6.6 -> v0.6.6

本次未发现高于 `v0.6.6` 的 stable 版本。GitHub Releases 仍为空；GitHub Tags 最新 stable 为 `v0.6.6`；npm `@mindfoldhq/trellis` 的 `latest` dist-tag 仍为 `0.6.6`。`0.6.0-beta.23` 和 `0.6.0-rc.0` 属于 prerelease，按 stable-only 策略忽略。

来源依据：

- GitHub tags：<https://api.github.com/repos/mindfold-ai/trellis/tags?per_page=30>
- GitHub compare：<https://github.com/mindfold-ai/trellis/compare/v0.6.6...v0.6.6>
- npm metadata：<https://registry.npmjs.org/@mindfoldhq/trellis>

Release 汇总：

- 无新 stable release notes 需要汇总。

破坏性变更：

- 无新 stable 版本，未发现新增破坏性变更。

迁移说明：

- 无需迁移。

对 agent harness workflow 的影响分析：

- 本轮没有新的 Trellis stable release 概念需要修改 AGENTS、onboard script 或 Skill 模板。
- 现有工作区中 `README.md`、`README.html` 和 `kuno-workflow-onboard-skills/templates/project/.gitignore` 的 OMP `.gitignore` 相关 diff 是本轮开始前已存在的改动；本次自动化不回滚、不覆盖，只在 `UPDATE.md` 中记录当前版本检查结果。

## GitNexus

### v1.6.9 -> v1.6.9

本次未发现高于 `v1.6.9` 的 stable 版本。GitHub Releases 最新 stable 仍为 `v1.6.9`，npm `gitnexus` 的 `latest` dist-tag 仍为 `1.6.9`；`v1.6.10-rc.15` / `1.6.10-rc.15` 是当前观察到的最新 release candidate，按 stable-only 策略忽略。

来源依据：

- GitHub release：<https://github.com/abhigyanpatwari/GitNexus/releases/tag/v1.6.9>
- GitHub releases：<https://api.github.com/repos/abhigyanpatwari/GitNexus/releases?per_page=20>
- npm metadata：<https://registry.npmjs.org/gitnexus>

Release 汇总：

- 无新 stable release notes 需要汇总。
- 已记录的 `v1.6.9` 仍是多仓 API tracing、PDG、taint、HTTP route / consumer extraction、默认 workspace index 跟随当前分支等相关 stable 版本。

破坏性变更：

- 无新 stable 版本，未发现新增破坏性变更。

迁移说明：

- 无需迁移。
- `v1.6.10-rc.*` 中的 CodeBuddy / Qoder integrations、Codex setup、plugin marketplace、proxy-blocked install、FTS self-heal、MCP 行号显示等内容仍为 RC 观察项，不写入 stable-only 长期规则。

对 agent harness workflow 的影响分析：

- 当前 AGENTS 模板已经覆盖 GitNexus MCP 可用性、索引 stale、metadata 位置、HTTP route / consumer、跨仓调用链、PDG / taint / trace、optional grammar 与索引完整性风险。
- 本轮没有 stable release-note 依据触发新的 GitNexus 模板修改。

## Playwright

### v1.61.1 -> v1.61.1

本次未发现高于 `v1.61.1` 的 stable 版本。GitHub Releases 最新 stable 为 `v1.61.1`，npm `playwright` 和 `@playwright/test` 的 `latest` dist-tag 均为 `1.61.1`；`next` dist-tag 为 `1.62.0-alpha-2026-07-10`，`beta` dist-tag 为 `1.61.1-beta-1782889362000`，按 stable-only 策略忽略。

来源依据：

- GitHub release：<https://github.com/microsoft/playwright/releases/tag/v1.61.1>
- GitHub releases：<https://api.github.com/repos/microsoft/playwright/releases?per_page=20>
- npm metadata：<https://registry.npmjs.org/playwright>
- npm metadata：<https://registry.npmjs.org/@playwright/test>

Release 汇总：

- 无新 stable release notes 需要汇总。

破坏性变更：

- 无新 stable 版本，未发现新增破坏性变更。

迁移说明：

- 无需迁移。

对 agent harness workflow 的影响分析：

- 当前 AGENTS / README / `project-validation` 规则已经覆盖 Playwright CLI、Playwright MCP、正式 HTML 报告、同 stem 中文 Markdown 汇总、stdout-only 诊断和 runner 临时目录边界。
- 本轮没有 release-note 依据触发新的 Playwright 模板修改。

## Maestro

### cli-2.6.1 -> cli-2.6.1

本次未发现高于 `cli-2.6.1` 的 stable CLI 版本。GitHub Releases 最新 stable 为 `cli-2.6.1`；tags 最新版本对应 `v2.6.1` / `cli-2.6.1`；raw changelog 的 `Unreleased` 为空。

来源依据：

- GitHub release：<https://github.com/mobile-dev-inc/Maestro/releases/tag/cli-2.6.1>
- GitHub releases：<https://api.github.com/repos/mobile-dev-inc/Maestro/releases?per_page=20>
- Raw changelog：<https://raw.githubusercontent.com/mobile-dev-inc/Maestro/main/CHANGELOG.md>

Release 汇总：

- 无新 stable release notes 需要汇总。

破坏性变更：

- 无新 stable 版本，未发现新增破坏性变更。

迁移说明：

- 无需迁移。

对 agent harness workflow 的影响分析：

- 当前 AGENTS / README / `maestro-mobile-e2e` 规则已经覆盖 Java 17+、Maestro CLI / MCP、flow 资产、正式报告和 stdout-only run 边界。
- 本轮没有 release-note 依据触发新的 Maestro 模板修改。
