# UPDATE

本次检查以 `ENTRYPOINT.md` 的“当前使用版本”为固定起点；未回写任何基线版本。检查时间：2026-07-31。

## Codex v0.146.0 -> v0.146.0

- **版本选择**：GitHub Releases 的最新非 prerelease 为 `rust-v0.146.0`（2026-07-29）；按 `v` / `rust-v` 语义规范化后等同于基线 `v0.146.0`。`rust-v0.147.0-alpha.2` 为 prerelease，已按 `stable-only` 排除。
- **Release 汇总**：没有跨越 stable 版本，因此无需汇总新增 release notes。
- **破坏性变更与迁移**：未发现需评估的 stable 升级；无迁移动作。
- **Agent harness workflow 影响**：无。未修改规则、模板或安装行为。
- **依据**：[GitHub Releases](https://api.github.com/repos/openai/codex/releases?per_page=100)。

## Trellis v0.6.10 -> v0.6.11

- **版本选择**：GitHub Releases 未提供可用于本次 stable 比对的 release body；Tags 中 `v0.6.11` 是高于基线的最新 stable tag。`v0.7.0-beta.0` 和 `v0.7.0-beta.1` 为 prerelease，已按 `stable-only` 排除。
- **变更汇总**：`v0.6.10...v0.6.11` 的官方 compare 包含 12 个提交。面向使用者的修复包括：Pi 子代理继承调用会话模型并支持 max thinking；Channel 暴露 Codex turn failure、在已完成 turn 后执行 idle timeout；hooks 独立按 UTF-8 解码 stdin；polyrepo 自动扫描和 Git status 探测设定边界；CLI 从 Trellis 受管文件识别已配置的平台。该范围还包含 Python 3.9 编译与 basedpyright 的内部 CI 校验。
- **破坏性变更与迁移**：目标 tag 的 `0.6.11` migration manifest 明确为 `breaking: false`、`recommendMigrate: false`，`migrations` 为空。升级后应运行 `trellis update` 刷新 Pi 与 hook 模板；不需要 `--migrate`。
- **平台与 workflow 依据**：目标 tag 的 `.trellis/config.yaml` 仍将 `codex.dispatch_mode` 明确限定为 Codex-only，并保留 Channel worker guard。目标 tag 同时包含 `.codex/config.toml`、`.omp/extensions/trellis/index.ts` 和 `.omp/agents/trellis-implement.md`；前者约束 Codex 的递归深度，后两者定义 OMP 会话上下文与由主会话调度的 OMP role agent。`.trellis/workflow.md` 仅定义共享 workflow gate。当前会话 host 是 OMP，但 upstream 的静态 tag 工件本身不能替代运行时平台判定；因此没有根据静态文件形成或修改平台调度规则。
- **Agent harness workflow 影响**：现有 `AGENTS.global.md` 已要求项目升级 Trellis 后先运行 `trellis update`，并已区分 Codex dispatch、OMP worker/agent 和显式 Channel runtime。此次是既有行为的可靠性修复，不是功能首次引入或默认值变化；不修改根 `AGENTS.md`、模板或 Skill。
- **依据**：[Tags](https://api.github.com/repos/mindfold-ai/trellis/tags?per_page=100)、[官方 compare](https://api.github.com/repos/mindfold-ai/trellis/compare/v0.6.10...v0.6.11)、[0.6.11 migration manifest](https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.11/packages/cli/src/migrations/manifests/0.6.11.json)、[target workflow](https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.11/.trellis/workflow.md)、[target config](https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.11/.trellis/config.yaml)、[Codex integration](https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.11/.codex/config.toml)、[OMP extension](https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.11/.omp/extensions/trellis/index.ts)、[OMP implement agent](https://raw.githubusercontent.com/mindfold-ai/trellis/v0.6.11/.omp/agents/trellis-implement.md)。

## GitNexus v1.6.9 -> v1.6.9

- **版本选择**：Tags 中最新 stable 仍为 `v1.6.9`。`v1.6.10-rc.*` 为 prerelease，已按 `stable-only` 排除。
- **Release 汇总**：没有跨越 stable 版本。
- **破坏性变更与迁移**：无。
- **Agent harness workflow 影响**：无。现有 GitNexus MCP、索引刷新和 advisory fallback 规则不变。
- **依据**：[GitHub Tags](https://api.github.com/repos/abhigyanpatwari/GitNexus/tags?per_page=100)。

## Playwright v1.62.0 -> v1.62.1

- **版本选择**：GitHub Releases 的最新 stable 为 `v1.62.1`（2026-07-30）。
- **Release 汇总**：`v1.62.1` 修复了 1.62 引入的 TypeScript 配置解析回归：bare-specifier `tsconfig` extends 不再沿 `node_modules` 向上解析，目录形式的 project references 不能解析；两项在 1.62 中均可能致命。还修复了 accessibility snapshot 中嵌套 `aria-hidden` SVG 导致 button name 丢失、`page.evaluate()` branded primitive 参数类型检查，以及 image-type actionable element 未在 snapshot 呈现的问题。
- **破坏性变更与迁移**：官方 release 仅列出 bug fixes；未声明破坏性变更或迁移步骤。
- **Agent harness workflow 影响**：对使用 TypeScript project references 或扩展 `tsconfig` 的项目，升级可消除 1.62.0 的构建/测试阻断。现有规则已经将 Playwright CLI 定义为可重复 Web 回归执行器、将 MCP snapshot 作为探索/诊断证据；这些边界不变。没有长期通用规则需要修改。
- **依据**：[v1.62.1 release](https://api.github.com/repos/microsoft/playwright/releases/tags/v1.62.1)。

## Maestro cli-2.7.0 -> cli-2.7.0

- **版本选择**：GitHub Releases 的最新 stable 为 `cli-2.7.0`（2026-07-20），与基线相同。
- **Release 汇总**：没有跨越 stable 版本。
- **破坏性变更与迁移**：无。
- **Agent harness workflow 影响**：无。Java、Maestro CLI/MCP、flow 与正式报告边界保持不变。
- **依据**：[GitHub Releases](https://api.github.com/repos/mobile-dev-inc/Maestro/releases?per_page=100)。

## 本仓库规则影响分析

- **关键词扫描范围**：已扫描根 `AGENTS.md`、版本化 automation prompt、Onboard catalog / schema / 文档 / 脚本、全局与项目 AGENTS 模板，以及 bundled Skills。Trellis 的 `trellis update`、`dispatch_mode`、subagent、hook、Channel 与 OMP 关键词均已命中既有边界；Playwright 的 CLI/MCP、accessibility snapshot 与 `page.evaluate` 使用边界也已命中既有定位。
- **处理决定**：Trellis `v0.6.11` 只修复既有能力，target manifest 不要求 migration；Playwright `v1.62.1` 只修复 1.62.0 回归。两者均不引入新的长期 workflow、配置或兼容性规则，因此只新增本次可追溯的版本分析，未修改规则、Skill、catalog、安装器或测试资产。
- **文档维护判断**：本次仅新增自动化检查记录 `UPDATE.md`，不改变用户可见能力、安装方式、工作流边界或验证契约；`CHANGELOG.md`、`README.md`、`README.html` 与版本化 automation prompt 无需改动。未读取或写入 Orca live automation；该行为仅由显式 `sync` / `同步` 触发。
- **Trellis 阶段**：本仓库不存在 `.trellis/workflow.md`，未执行 Trellis 阶段。
