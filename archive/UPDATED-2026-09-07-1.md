# UPDATE

## Codex v0.153.2 -> v0.153.4

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v0.153.2`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `rust-v0.153.4`（2026-09-04T23:25:48Z，`prerelease=false`，`draft=false`）。npm `@openai/codex` 的 `dist-tags.latest=0.153.4`。其后仅有 `rust-v0.154.0-alpha.3` 以及 npm `alpha=0.154.0-alpha.3`；通道策略为 `stable-only`，不把 alpha 当作目标版本。本区间比较 `v0.153.2` 到 `v0.153.4`，中间包含已发布的 stable patch `rust-v0.153.3`。

### 证据来源

- 有依据：
  - `https://api.github.com/repos/openai/codex/releases/latest`：`tag=rust-v0.153.4`，`prerelease=false`，`draft=false`，发布时间 `2026-09-04T23:25:48Z`。
  - npm `@openai/codex`：`dist-tags.latest=0.153.4`，`alpha=0.154.0-alpha.3`。
  - 中间 stable patch `https://api.github.com/repos/openai/codex/releases/tags/rust-v0.153.3`：`prerelease=false`，`draft=false`，发布时间 `2026-09-04T19:01:32Z`。Release body：把 GPT-6-Astra 加入 Amazon Bedrock model picker（Mantle 与 Runtime 的 global/US 路由，`#42805`）；修正 Astra 异步澄清问题的 guidance，要求使用受支持 tool 且只接受文本（`#42809`）。Full Changelog 指向 `rust-v0.153.2...rust-v0.153.3`。
  - GitHub compare `rust-v0.153.2...rust-v0.153.3`：`ahead_by=3`，`behind_by=1`，`total_commits=3`，`files=13`（未触顶，可作为该 patch 的完整文件清单）。提交：`#42805` Add GPT-6-Astra to Amazon Bedrock catalogs、`#42809` Update Astra async-question guidance、release notes。13 个文件：`codex-rs/Cargo.toml`、`model-provider-info/src/lib.rs`、`amazon_bedrock/catalog.rs`、`runtime_catalog.rs`、`runtime_catalog_tests.rs`、`provider.rs`、`models-manager/models.json`、TUI Bedrock snapshot / 测试 6 个文件。
  - `models.json` blob：`rust-v0.153.2` size=515129、SHA `451b3df440ddcbd58de646e607ae1cefb04955ad`；`rust-v0.153.3` size=515129、SHA `6c5fc40c4fa6b9f1ea02c6cb319ac3cdb8d656ff`。两份 tagged JSON 中 `gpt-6-astra` 仍为 `visibility=hide`、`priority=1`、`supported_in_api=true`、`minimal_client_version=0.153.0`。`visibility=list` 集合仍由 `gpt-5.6-sol`（priority 6）起头。
  - 目标 tag `https://api.github.com/repos/openai/codex/releases/tags/rust-v0.153.4` Release body：修复 Astra 在 bundled model picker 中的可见性，并在未显式配置模型时把它作为 bundled 默认（`#42874`）；把 Astra 异步提问 guidance 限定为 session 中该 tool 可用时才使用（`#42878`）。Full Changelog 指向 `rust-v0.153.3...rust-v0.153.4`。
  - GitHub compare `rust-v0.153.3...rust-v0.153.4`：`ahead_by=3`，`behind_by=1`，`total_commits=3`，`files=3`（未触顶）：`codex-rs/Cargo.toml`、`codex-rs/models-manager/models.json`、`codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__model_selection_popup.snap`。提交：`#42874`、`#42878`、release notes。
  - 目标 tag `models.json` size=515145、SHA `698da6fb7a825cd3ede1696e4ce8579ef5c42c02`。本轮直接解析：`gpt-6-astra` 的 `visibility` 从 `hide` 改为 `list`，`priority` 仍为 `1`，是 `visibility=list` 集合中 priority 最低的条目；`display_name=GPT-6-Astra`，`supported_in_api=true`，`minimal_client_version=0.153.0`，Fast 档文案仍为 `2x speed, increased usage`。其余 listed 模型顺序不变：`gpt-5.6-sol` / `terra` / `luna` / `gpt-5.5` / `gpt-5.2`。
  - 目标 tag `codex-rs/model-provider/src/amazon_bedrock/catalog.rs`：size=11305，SHA `ff2a49307b9d0b3872492cc18814ac1900a42ab8`。源码把 `gpt-6-astra` 映射到 `AMAZON_BEDROCK_GPT_6_ASTRA_MODEL_ID`，展示名 `GPT-6-Astra`。这是 Bedrock catalog 映射，不是 Trellis / Onboard 配置。
  - 目标 tag `docs/config.md` 仍为 726 字节 stub（blob SHA `d35b0a87e3c2286a4f0d87a62414d0cec4e742b4`），与 `v0.153.2` 相同。
  - 目标 tag `codex-rs/config/src/config_toml.rs` blob SHA `5f0cc99a4740803805791250d7e87332673c6d85`，`codex-rs/core/config.schema.json` blob SHA `24d7988b04e801005ee926ea8381ae21f6ff1ab6`，均与 `v0.153.2` / `v0.153.0` 相同。对该 schema 检索 `dispatch_mode` 出现次数为 0。
- 缺失：
  - 目标 tag `docs/config.md` 未展开 GPT-6-Astra、Bedrock picker 或异步提问 tool。用户面向行为以 Release body、完整 compare 与 tagged `models.json` / Bedrock catalog 为准。
  - Trellis 式 workflow 模板：对目标 tag **分别**查询 `docs/config.md`（存在，726 字节 stub）、`.trellis/workflow.md`（HTTP 404）、根 `workflow.md`（HTTP 404，单独查询，不依赖前一项）。两项 workflow 路径均不存在。
  - Trellis 式 upgrade / migration manifest：目标 tag 不存在。`sdk/typescript/src/HookMigration.ts` 与 `sdk/typescript/src/SubagentMigration.ts` 在 `rust-v0.153.4` 上均为单独 contents 查询 HTTP 404。
  - 本仓库不是 Trellis 项目，不存在生成的 `.codex/**` 或 `.omp/**`；不得用未发布 `main` 或当前 OMP host 推断调度规则。
  - 未读取 `v0.154.0-alpha.*` 的 body / compare；alpha 不能作为规则依据。
  - `v0.153.3` / `v0.153.4` compare 的 `behind_by=1` 不能证明未列出的 commit 已合入；Astra 可见性与 Bedrock 映射以目标 tag 文件内容为准。

### 发布汇总

`v0.153.2 -> v0.153.3`（独立 patch；依据本轮 Release body、完整 compare 13 文件与 tagged 源码）：

- 新功能：Amazon Bedrock Mantle / Runtime（global/US）catalog 增加 GPT-6-Astra（`#42805`）。这是 Bedrock provider catalog 扩展，不是默认安装、不是 Trellis dispatch。
- 既有能力修复：Astra 异步澄清问题的 guidance 改为使用受支持 tool，且只接受文本（`#42809`）。这是模型 guidance，不是新的用户配置键、hook 名或 MCP 安装命令。
- `models.json` 中 Astra 在本 patch 仍为 `visibility=hide`；bundled picker 默认模型未在本 patch 改变。
- Chores / Bedrock TUI snapshot 不进入本仓库长期规则。

`v0.153.3 -> v0.153.4`（独立 patch；依据完整 compare 3 文件、Release body 与 tagged JSON）：

- 默认值变化：GPT-6-Astra 在 bundled model picker 中 `visibility` 从 `hide` 改为 `list`，`priority=1` 使其成为未显式配置模型时的 bundled 默认（`#42874`）。这是 Codex 产品 catalog 默认，不是本仓库 / Onboard / AGENTS 模板的默认模型设置。
- 既有能力修复：Astra 异步提问 guidance 仅在 session 中该 tool 可用时使用（`#42878`）。compare 未改 guidance 源文件，只改 `models.json` 与 picker snapshot；以 Release body 为准，不写成无可追溯。
- Fast 档文案仍为 `2x speed, increased usage`，与 `v0.153.2` 相同。

### 破坏性变更 / 迁移

- 两份 Release body 均未列出强制迁移步骤。
- GPT-6-Astra 现在是 Codex bundled picker 默认，但仍不要把它写进本仓库默认模型、Onboard 安装或可复用 AGENTS 模板。
- Bedrock catalog 映射只影响 Amazon Bedrock provider；不要把它写成通用模型或 Trellis 配置。
- 异步提问 tool 可用性是 session 级模型 guidance，不是新的 MCP / hook schema。

### 平台调度核验

`v0.153.3` / `v0.153.4` Release body 指向模型 catalog、Bedrock picker 与 Astra guidance。两段 compare 文件列表完整，仍按第 6.1 步用目标 tag 直接查询核验，不得只凭 release body。

有效配置：

- `docs/config.md` 为 stub，且 SHA 与 `v0.153.2` 相同。
- 目标 tag `config_toml.rs` 与 `config.schema.json` blob SHA 与 `v0.153.2` 相同；其中没有 `codex.dispatch_mode` / `dispatch_mode` 键。
- GPT-6-Astra 是 Codex 模型 catalog 条目；`v0.153.4` 起 bundled picker 可见且为未显式配置时的默认。不是 Trellis / Onboard 配置。
- Amazon Bedrock catalog 是 Codex provider 映射，不是 Trellis role subagent 或 OMP worker。

workflow 模板：

- 目标 tag `.trellis/workflow.md`：单独 contents 查询 HTTP 404。
- 目标 tag 根 `workflow.md`：单独 contents 查询 HTTP 404。该项不存在。

migration / upgrade manifest：

- 目标 tag 无 Trellis 式 upgrade / migration manifest。该项不存在。
- `HookMigration.ts` / `SubagentMigration.ts`：对 `rust-v0.153.4` contents 查询均为 HTTP 404。

分类：

- `#42805`：既有 Bedrock catalog 的能力扩展（新增 Astra 映射）。不是默认安装，不是新的 dispatch 模式。
- `#42809` / `#42878`：既有 Astra 异步提问 guidance 的 bug fix。不是 hook 系统首次引入，不是第二写入执行者。
- `#42874`：Codex bundled 模型 catalog 的默认值变化（Astra `visibility=hide` → `list`，成为未显式配置时的默认）。不是 Trellis / Channel / Onboard 默认值变化。

平台集成：本仓库无生成的 `.codex/**` / `.omp/**`。不得用当前 OMP host 或未发布 `main` 形成调度规则。不得把 Codex Bedrock catalog、GPT-6-Astra 或 OMP `node_repl` MCP 互相套用。

处理决定：缺少 Trellis workflow 模板与 Trellis migration manifest，不得形成或更新平台调度规则。既有规则继续有效：`.trellis/**` 只定义共享 workflow gate；Codex `dispatch_mode` / Inline fallback 仅适用于 Codex host；OMP 使用生成的 `task` worker；Channel 保持显式确认。不把 GPT-6-Astra 写入可复用模板。

### 影响分析

扫描关键词：`GPT-6-Astra`、`gpt-6-astra`、`Amazon Bedrock`、`request_user_input_async`、`visibility=list`、`dispatch_mode`、`codex.dispatch_mode`、`SubagentStart`、Channel、`HookMigration`、`SubagentMigration`。

命中结果：

- 允许扫描路径（`AGENTS.md`、版本检查 prompt、`catalog.json`、`catalog.schema.json`、`sbtd-workflow-onboard/SKILL.md`、`REFERENCE.md`、`onboard.py`、`AGENTS.global.md`、`AGENTS.project.md`、`templates/skills/**`、`install.sh`、`install.ps1`、项目 `.gitignore`、`tests/**`）未命中 `GPT-6-Astra`、`gpt-6-astra`、`Amazon Bedrock`。
- `AGENTS.md`、版本检查 prompt、`AGENTS.global.md`、`AGENTS.project.md`、`trellis-workflow`、`trellis-channel`、`tests/test_workflow_contracts.py` 命中的仍是既有 Trellis / Codex dispatch / hook / Channel 核验规则，不是本区间新的模型 catalog 命令。
- `onboard.py` / `install.sh` / `install.ps1`、`catalog.json`、`catalog.schema.json`、项目 `.gitignore`：未锁定 GPT-6-Astra 或 Bedrock catalog。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。不把 `v0.153.3`、`v0.153.4` 或 `v0.154.0-alpha.*` 写进长期规则。Codex bundled 默认模型是产品 catalog 细节，不进入可复用全局 / 项目 AGENTS 模板。

## Trellis v0.6.16 -> v0.6.16

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v0.6.16`。GitHub Releases API 对该仓库仍返回空列表（`length=0`），因此用 tags + npm 交叉确认。最新 stable tag 仍为 `v0.6.16`（commit `88f4834449da9b4f607ec05e322408a0aa66f2ce`）。更高 tag 仅有 `v0.7.0-beta.*`，最新见到 `v0.7.0-beta.3`（commit `53ae2047716c7043583b1cf6e645462559b70960`）；npm `@mindfoldhq/trellis` 的 `latest=0.6.16`，`beta=0.7.0-beta.3`。通道策略为 `stable-only`，不跳到 beta。本区间无新 stable。

### 证据来源

- 有依据：
  - Git tags：`v0.6.16` 存在；更高仅为 `v0.7.0-beta.0` 到 `v0.7.0-beta.3`。
  - npm：`@mindfoldhq/trellis` 的 `dist-tags.latest=0.6.16`，`beta=0.7.0-beta.3`。
- 缺失：
  - GitHub Release body：`/releases` 为空。
  - 本次无新 stable 区间，未再展开 docs changelog、compare、migration manifest 或平台集成。
  - 未读取 `v0.7.0-beta.*`；beta 不能作为规则依据。
  - 本仓库无 `.trellis/`，也没有生成的 `.codex/**` / `.omp/**`。

### 破坏性变更 / 迁移

无。无需迁移。

### 平台调度核验

无新 tagged stable 证据。不形成或更新平台调度规则。

### 影响分析

本地已有 `task.py rename`、`trellis ablate` / `restore`、空 jsonl 启动门禁、`task_error`、OMP `skip_keyword` 与 Codex/OMP 调度边界。无新 stable 关键词需要改规则。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。

## GitNexus v1.6.10 -> v1.6.11

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v1.6.10`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `v1.6.11`（2026-09-04T19:28:22Z，`prerelease=false`，`draft=false`）。npm `gitnexus` 的 `dist-tags.latest=1.6.11`，`rc=1.6.12-rc.4`。其后只有 `v1.6.12-rc.*`；通道策略为 `stable-only`，不把 RC 当作目标版本。本区间比较 `v1.6.10` 到 `v1.6.11`。

### 证据来源

- 有依据：
  - `https://api.github.com/repos/abhigyanpatwari/GitNexus/releases/latest`：`tag=v1.6.11`，`prerelease=false`，`draft=false`，发布时间 `2026-09-04T19:28:22Z`。
  - npm `gitnexus`：`dist-tags.latest=1.6.11`，`rc=1.6.12-rc.4`。
  - 目标 tag Release body（完整，可作为本区间变更依据）：Zig 一等语言支持；`analyze --watch` 与 `gitnexus auto-sync`；`gitnexus watch` 保留且不启动任一者；更新通知与 `gitnexus update`；Spring / JVM / 合约面扩展；多项可靠性修复。官方声明相对 `v1.6.10` **无 breaking changes**；Node 下限仍为 `^22.18.0 || >=24.11.0`。升级后需重新 `gitnexus analyze`，因为新增 Zig、Spring destinations、Kotlin decorator routes 和合约边。
  - 目标 tag `README.md`：`gitnexus analyze [path] --watch` 要求 Git 仓库，前台运行，Ctrl+C 停止，不 pull remotes；`gitnexus auto-sync` 从 `$GITNEXUS_HOME/watch_config.yml` 做远程 clone/pull + analyze，前台循环。Release body 另写明裸 `gitnexus watch` 只打印二者分工并启动 neither。
- 缺失：
  - 未再展开 GitHub compare 文件列表；Release body 已足够判断命令边界，不以截断 compare 作为缺失证明。
  - 未读取 `v1.6.12-rc.*`；RC 不能作为规则依据。
  - 本仓库无 `.gitnexus/` 索引，也没有生成的 `.codex/**` / `.omp/**`。

### 发布汇总

- 新功能：Zig 作为可选 grammar 语言；缺失 native binding 时跳过 `.zig` 而不是安装失败。
- 新功能：`analyze --watch` 做本地增量再索引；`gitnexus auto-sync` 按配置远程 clone/pull。二者都是长期前台进程。
- 命令边界：裸 `gitnexus watch` 是保留入口，只打印分工，不启动 watcher。
- 新功能：更新通知（可用 `GITNEXUS_NO_UPDATE_NOTIFIER` 关闭）与 `gitnexus update` 全局安装 latest。
- 建模扩展：Spring destinations、Kotlin decorator routes、AsyncAPI / GraphQL / 生成客户端合约面。这些是索引边，不是 SBTD 工作流步骤。
- 既有能力修复：`detect_changes` 不再把零符号结果叫干净树；`status` 以覆盖文件新鲜度为准，不以 dirty working tree 单独判定；analyze 不再覆盖自定义 GitNexus skills；incremental analyze 可复用 derived layers。
- 官方：无 breaking changes。升级后必须重新 analyze 才能获得新边。

### 破坏性变更 / 迁移

- Release body 明确无 breaking changes。
- 既有索引不会自动带上 Zig / Spring destinations / Kotlin decorator / 新合约边；升级后按项目约定重新 `gitnexus analyze`。
- 不要默认启动 `analyze --watch` 或 `auto-sync`；它们是长期前台进程，会写入索引。
- 不要把 `gitnexus watch` 解释为已启动的 watcher。
- `gitnexus update` 会全局安装 latest，不是本仓库 Onboard / sync 路径；不要静默执行。

### 平台调度核验

GitNexus 本区间不涉及 Trellis dispatch、Channel 或 Codex/OMP role worker。Release body 中的 Cursor hooks / generated agent block 是 GitNexus 自身 hook / skill 生成物，不是 Trellis `SubagentStart` 或 Channel。

处理决定：不形成或更新平台调度规则。不得把 GitNexus Codex plugin 或 generated skills 写成 Trellis role subagent 或 OMP worker。

### 影响分析

扫描关键词：`gitnexus watch`、`analyze --watch`、`auto-sync`、`GITNEXUS_NO_UPDATE_NOTIFIER`、`gitnexus update`、`--no-parse-cache`、`detect_changes`、Zig。

命中结果：

- 允许扫描路径未命中 `gitnexus watch`、`analyze --watch`、`auto-sync`、`GITNEXUS_NO_UPDATE_NOTIFIER`、`gitnexus update`、`--no-parse-cache`。
- `AGENTS.global.md` / README 已有 stale 后 CLI 重新 analyze、optional grammar 跳过、不静默新增 hook、不默认 `--self-commit`、plugin marketplace 不能替代全局 `gitnexus-mcp`。`AGENTS.project.md` 只保留项目级 GitNexus 硬边界，不复制全局命令边界。
- `onboard.py` / 安装器 / catalog 未锁定 watch / auto-sync。

处理决定：最小化更新长期命令边界：只在 `AGENTS.global.md` 写明 `gitnexus watch` 是保留入口、不要默认启动 `analyze --watch` / `auto-sync`；索引刷新仍用一次性 `gitnexus analyze`。`AGENTS.project.md` 不复制该全局工具边界。不把 Zig、Spring 建模、更新通知或 `gitnexus update` 写进模板。不写回 `ENTRYPOINT.md`。

## Playwright v1.62.1 -> v1.63.0

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `v1.62.1`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `v1.63.0`（2026-09-04T22:40:31Z）。npm `playwright` 与 `@playwright/test` 均为 `latest=1.63.0`。更高仅有 `next=1.64.0-alpha-2026-09-06` 与 `beta=1.63.0-beta-1788559919000`；`stable-only` 忽略 alpha / beta。

### 证据来源

- 有依据：
  - GitHub `/releases/latest`：`tag=v1.63.0`，`prerelease=false`，`draft=false`，发布时间 `2026-09-04T22:40:31Z`。
  - npm `playwright` / `@playwright/test`：`latest=1.63.0`。
  - Release body 完整：test locks、跨 frame `frameLocator()`、`locator.visible()` 替代 `:visible`、step subtitle/params、trace aria/screen snapshots、`--add-reporter`、`omitTags`、实验 CT 包停止更新、不再支持 Ubuntu 20.04。
- 缺失：本次未再展开 compare 文件列表；Release body 已覆盖 API / 兼容性公告。

### 发布汇总

- 新功能：named `lock` 让共享资源测试跨文件 / worker / project 串行。
- 新功能：无 selector 的 `page.frameLocator()` 在子树任意 frame 搜索。
- 新功能：`locator.visible()` 作为 `:visible` CSS 伪类的推荐替代。
- 新功能：step `subtitle` / `params`；trace 可选 aria/screen snapshots。
- CLI：`--add-reporter` 追加 reporter，不替换 `--reporter`；`npx playwright install --no-remove` 保留其他安装的浏览器。
- 公告：`@playwright/experimental-ct-react` / `react17` / `vue` 不再更新；Ubuntu 20.04 不再支持；Linux arm64 改下 Chrome for Testing。
- 浏览器：Chromium 153 / Firefox 155 / WebKit 26.6。

### 破坏性变更 / 迁移

- 实验组件测试包停止更新；本仓库模板不引用这些包。
- Ubuntu 20.04 不再支持；本仓库不锁定该发行版。
- `:visible` 仍可用，但上游推荐 `locator.visible()`。本仓库模板未写 `:visible`。
- `--add-reporter` 不改变既有“`--reporter=list` 只算诊断”的正式报告 gate。

### 影响分析

扫描关键词：`locator.visible`、`:visible`、`test locks`、`experimental-ct`、`Ubuntu 20.04`、`--add-reporter`、`omitTags`、`frameLocator()`、`ariaSnapshotJSON`、`--no-remove`。

命中结果：允许扫描路径未命中上述关键词。本地 Playwright CLI / MCP / 报告路径规则已覆盖当前职责边界。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。不把 `v1.63.0` API 细节写进长期规则。

## Maestro cli-2.10.0 -> cli-2.10.0

### 版本结论

`ENTRYPOINT.md` 当前使用版本为 `cli-2.10.0`。GitHub `/releases/latest` 与最新非 draft、非 prerelease 均为 `cli-2.10.0`（2026-08-31T17:15:09Z）。其后没有更新的 stable CLI tag。

### 证据来源

- 有依据：
  - GitHub Releases 首页最新 stable 为 `cli-2.10.0`。
  - `https://api.github.com/repos/mobile-dev-inc/Maestro/releases/latest`：`tag=cli-2.10.0`，`prerelease=false`，`draft=false`。
- 缺失：本次无新 stable 区间，未再展开 changelog。Maestro 未用 npm dist-tag 交叉确认。

### 破坏性变更 / 迁移

无。无需迁移。

### 影响分析

本地 Maestro Java 17+ / CLI / MCP / 报告路径规则已覆盖当前 stable。无新版本关键词需要改规则。

处理决定：不修改本仓库规则，不写回 `ENTRYPOINT.md`。
