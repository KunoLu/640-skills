# UPDATE

## Codex

### v0.139.0 -> v0.139.0

### 检测来源

- GitHub Releases：`openai/codex` 最新稳定 release 为 `rust-v0.139.0`，发布时间为 `2026-06-09T20:13:29Z`。
- npm metadata：`@openai/codex` 的 `latest` 为 `0.139.0`，`alpha` 为 `0.140.0-alpha.2`。
- 版本通道策略：`stable-only`，因此 `0.140.0-alpha.2` 及对应平台 alpha dist-tags 不进入本次比较。

### Release 汇总

`ENTRYPOINT.md` 当前基线已经是 `v0.139.0`，本次没有发现高于该基线的稳定版 Codex release。`v0.139.0` release notes 本身包含 web search、MCP schema、`codex doctor`、plugin marketplace、sandbox escalation、TUI reset 等改动，但这些已经属于当前基线，不再生成新的升级区间。

### 破坏性变更

未发现当前基线之后的新稳定版破坏性变更。

### 迁移说明

无需迁移。每日自动化不写回 `ENTRYPOINT.md`，当前版本字段保持 `v0.139.0`。

### 对 agent harness workflow 的影响分析

本次没有新稳定版需要沉淀为长期规则。`AGENTS.md`、`agents/AGENTS.global.md`、`agents/AGENTS.project.md` 和 `skills/**/SKILL.md` 中已有 sandbox、MCP、plugin、web search 相关边界，未发现需要基于本次检测新增或调整的规则。

## Trellis

### v0.6.0-beta.23 -> v0.6.0-beta.23

### 检测来源

- GitHub Releases：`mindfold-ai/trellis` 当前 releases 列表为空。
- GitHub Tags：最新标签为 `v0.6.0-rc.0`，同 beta 通道最新标签仍为 `v0.6.0-beta.23`。
- npm metadata：`@mindfoldhq/trellis` 的 `beta` 为 `0.6.0-beta.23`，`rc` 为 `0.6.0-rc.0`，`latest` 为 `0.5.19`。
- 版本通道策略：当前版本是 `v0.6.0-beta.23`，策略为 `same-prerelease-channel`，因此只比较 `v0.6.0-beta.x` 中更高 beta 序号；`v0.6.0-rc.0` 不主动纳入。

### Release 汇总

本次没有发现同 beta 通道内高于 `v0.6.0-beta.23` 的 Trellis 版本。GitHub release body 仍缺失，已用 GitHub tags 和 npm dist-tags 交叉确认；由于没有新 beta 区间，本次无需继续展开 compare、commit diff、migration manifest 或 tarball 结构分析。

### 破坏性变更

未发现当前 beta 基线之后的新 beta 通道破坏性变更。

### 迁移说明

无需迁移。`trellis update`、registry-backed spec templates 和 channel runtime agent 定义等长期规则已经在现有 AGENTS/Skill 中覆盖。

### 对 agent harness workflow 的影响分析

本次没有 accepted-channel 新版本，不修改 Trellis 相关 Skill 或 AGENTS 模板。`v0.6.0-rc.0` 作为 rc 通道版本被记录但不影响当前 same-prerelease-channel 策略。

## GitNexus

### v1.6.7 -> v1.6.7

### 检测来源

- GitHub Releases：`abhigyanpatwari/GitNexus` 最新稳定 release 为 `v1.6.7`，发布时间为 `2026-06-09T21:05:55Z`。
- npm metadata：`gitnexus` 的 `latest` 为 `1.6.7`，`rc` 为 `1.6.7-rc.13`。
- 版本通道策略：`stable-only`，因此 `1.6.7-rc.13` 不进入本次比较。

### Release 汇总

`ENTRYPOINT.md` 当前基线已经是 `v1.6.7`，本次没有发现高于该基线的稳定版 GitNexus release。`v1.6.7` release notes 本身包含 tree-sitter prebuild、`gitnexus uninstall`、MCP `list_repos` pagination、optional grammar lazy-load、FTS 和 query enrichment 等内容，但这些已经属于当前基线，不再生成新的升级区间。

### 破坏性变更

未发现当前基线之后的新稳定版破坏性变更。

### 迁移说明

无需迁移。每日自动化不写回 `ENTRYPOINT.md`，当前版本字段保持 `v1.6.7`。

### 对 agent harness workflow 的影响分析

本次没有新稳定版需要沉淀为长期规则。`agents/AGENTS.global.md` 和 `agents/AGENTS.project.md` 已包含 `list_repos` 分页、`gitnexus uninstall` dry-run/`--force` 安全边界、optional grammar/prebuild 覆盖风险和大仓库索引完整性风险处理规则；无需进一步修改。
