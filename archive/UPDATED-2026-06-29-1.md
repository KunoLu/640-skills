# UPDATE

## Codex

### v0.142.2 -> v0.142.3

#### 版本结论

- 最新稳定版：`v0.142.3`，对应 GitHub release `rust-v0.142.3`，npm `@openai/codex` 的 `latest` dist-tag 也是 `0.142.3`。
- `0.143.0-alpha.29` 及对应平台包属于 alpha 预发布；当前 `ENTRYPOINT.md` 对 Codex 的策略是 `stable-only`，本次不纳入升级目标。
- 本次区间固定以 `ENTRYPOINT.md` 中的 `v0.142.2` 为起点，不读取或推进到其他历史 `UPDATE.md` 目标版本。

#### Release 汇总

- GitHub release 明确标注这是维护补丁：相对 `0.142.2` 没有用户可见变化。
- GitHub compare `rust-v0.142.2...rust-v0.142.3` 显示 2 个提交，主要涉及 `codex-rs` 版本 / provider 元数据和 Amazon Bedrock catalog 的模型条目更新。
- 结合 release body 与 compare，本次对 agent harness workflow 的可见影响很低：没有新的命令、配置项、审批边界、MCP 行为或工具调用流程要求。

#### 破坏性变更

- 未发现破坏性变更。
- 未发现需要修改现有 Codex 调用、审批、插件、MCP 或 sandbox 规则的 release-note 依据。

#### 迁移说明

- 不需要迁移。
- 每日自动化不修改 `ENTRYPOINT.md`；只有用户手动输入 `更新` 或 `update` 时，才允许把 `v0.142.3` 写回 Codex 当前使用版本。

#### 对 agent harness workflow 的影响分析

- 规则扫描关键词：`maintenance-only`、`Bedrock`、`GPT-5.6`、`model provider`、`tool search`、`system proxy`、`MCP tools`。
- 本地扫描范围：`AGENTS.md`、`ENTRYPOINT.md`、`README.md`、`README.html`、`kuno-workflow-onboard-skills/SKILL.md`、`kuno-workflow-onboard-skills/REFERENCE.md`、`kuno-workflow-onboard-skills/scripts/onboard.py`、`kuno-workflow-onboard-skills/templates/agents/AGENTS.global.md`、`kuno-workflow-onboard-skills/templates/agents/AGENTS.project.md`、`kuno-workflow-onboard-skills/templates/skills/**`。
- 命中结果：只命中既有 MCP 可见性和手动配置边界说明；没有 Bedrock、GPT-5.6 或 model provider 相关长期规则。
- 处理决定：不修改 AGENTS、Skill、README 或 onboard 脚本。Codex 维护补丁和 Bedrock catalog 元数据更新不足以形成本仓库长期通用规则。

#### 依据来源

- GitHub release：`https://github.com/openai/codex/releases/tag/rust-v0.142.3`
- GitHub compare：`https://github.com/openai/codex/compare/rust-v0.142.2...rust-v0.142.3`
- npm metadata：`https://registry.npmjs.org/@openai/codex`

## Trellis

### v0.6.5 -> v0.6.5

#### 版本结论

- 最新稳定版仍为 `v0.6.5`。
- GitHub Releases 当前没有 release 条目；GitHub tags 显示最新稳定 tag 为 `v0.6.5`，npm `@mindfoldhq/trellis` 的 `latest` dist-tag 也是 `0.6.5`。
- `beta` dist-tag 仍为 `0.6.0-beta.23`，`rc` dist-tag 仍为 `0.6.0-rc.0`；当前策略为 `stable-only`，不切换到 beta 或 rc 通道。

#### Release 汇总

- 本次相对 `ENTRYPOINT.md` 当前版本没有新稳定版本。
- 由于没有跨版本区间，本次不需要重新汇总 Trellis release notes、compare、migration manifest 或 npm tarball 内容。

#### 破坏性变更

- 未发现新的稳定版破坏性变更。

#### 迁移说明

- 不需要迁移。
- 每日自动化不修改 `ENTRYPOINT.md`。

#### 对 agent harness workflow 的影响分析

- 没有新稳定 release-note 依据，因此不修改 Trellis workflow、Channel、AGENTS 模板、README 或 onboard 行为。
- 昨日已同步到本仓库的 Trellis `v0.6.5` 相关长期规则保持不变。

#### 依据来源

- GitHub tags：`https://github.com/mindfold-ai/trellis/tags`
- npm metadata：`https://registry.npmjs.org/@mindfoldhq/trellis`

## GitNexus

### v1.6.8 -> v1.6.8

#### 版本结论

- 最新稳定版仍为 `v1.6.8`，npm `gitnexus` 的 `latest` dist-tag 仍为 `1.6.8`。
- GitHub Releases 最新可见条目是 `v1.6.9-rc.26`，npm `rc` dist-tag 也是 `1.6.9-rc.26`；这些均为 release candidate，当前策略为 `stable-only`，本次忽略。

#### Release 汇总

- 本次相对 `ENTRYPOINT.md` 当前版本没有新稳定版本。
- `v1.6.9-rc.*` 中的 Python taint model、LadybugDB/PDG 修复、多语言 HTTP consumer/provider 提取等内容仍停留在 rc 通道，不沉淀为稳定规则。本次只观察到 rc 序号推进到 `rc.26`，稳定通道没有变化。

#### 破坏性变更

- 未发现新的稳定版破坏性变更。

#### 迁移说明

- 不需要迁移。
- 不刷新 GitNexus 索引；本仓库没有 `.gitnexus/`，且本次只是配置摘录仓库的版本检查。

#### 对 agent harness workflow 的影响分析

- 没有新稳定 release-note 依据，因此不修改 GitNexus MCP、PDG、taint、trace、多分支索引或 transport 相关长期规则。
- 既有 `v1.6.8` 稳定规则仍适用。

#### 依据来源

- GitHub releases：`https://github.com/abhigyanpatwari/GitNexus/releases`
- npm metadata：`https://registry.npmjs.org/gitnexus`

## Playwright

### v1.61.1 -> v1.61.1

#### 版本结论

- 最新稳定版仍为 `v1.61.1`，GitHub release 与 npm `playwright` 的 `latest` dist-tag 一致。
- npm `next` dist-tag 为 `1.62.0-alpha-2026-06-28`，属于 alpha 预发布；当前策略为 `stable-only`，本次忽略。

#### Release 汇总

- 本次相对 `ENTRYPOINT.md` 当前版本没有新稳定版本。
- 当前稳定 release `v1.61.1` 是已知的 bug-fix patch，内容仍是 expect matcher、UI mode APIRequestContext bytes、Trace Viewer WebSocket 时间、Node 22.15 sync loader、pnpm workspace extensionless TS import 解析修复。

#### 破坏性变更

- 未发现新的稳定版破坏性变更。

#### 迁移说明

- 不需要迁移。
- 不修改 Playwright CLI、Playwright MCP、Web E2E 或 `web-ui-autotest-generator` 规则。

#### 对 agent harness workflow 的影响分析

- 没有新稳定 release-note 依据，因此不修改 Web / Mobile 验证工具边界、安装 gate、报告状态或测试资产路径规则。

#### 依据来源

- GitHub release：`https://github.com/microsoft/playwright/releases/tag/v1.61.1`
- npm metadata：`https://registry.npmjs.org/playwright`

## Maestro

### cli-2.6.1 -> cli-2.6.1

#### 版本结论

- 最新稳定 CLI release 仍为 `cli-2.6.1`。
- GitHub release 指向 `CHANGELOG.md#261`，raw changelog 顶部仍显示 `2.6.1` 为最新发布版本，`Unreleased` 暂无条目。

#### Release 汇总

- 本次相对 `ENTRYPOINT.md` 当前版本没有新稳定版本。
- 当前稳定 release `2.6.1` 的已知内容仍是 iOS cross-process sheets / hierarchy walk performance 修复、cross-origin iframe stale handling 修复，以及 CLI hint URL 更新。

#### 破坏性变更

- 未发现新的稳定版破坏性变更。

#### 迁移说明

- 不需要迁移。
- 不修改 Maestro Java gate、CLI/MCP 检测、flow 资产路径或 `.maestro/reports/` 报告规则。

#### 对 agent harness workflow 的影响分析

- 没有新稳定 release-note 依据，因此不修改 Maestro / Mobile E2E / Hybrid E2E 长期规则。

#### 依据来源

- GitHub releases：`https://github.com/mobile-dev-inc/Maestro/releases`
- Maestro changelog：`https://github.com/mobile-dev-inc/maestro/blob/main/CHANGELOG.md#261`
