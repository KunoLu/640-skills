# P2-01：迁移范围、备份责任与保留安排冻结

## 1. 授权与边界

- 任务：P2-01；唯一状态事实源为[主 PRD §14](sbtd-workflow-v2-trellis-removal-graft-migration-prd.md#14-优先级实施台账)。
- 开发起点：`main` `03541a97f3804f8966cd5c4ff3f579793f9d4175`；任务分支 `p2-01-migration-scope-freeze`。
- 用户授权：开始 P2 与 P2-01 冻结。没有 apply、独立部署、cleanup、本机 workflow sync、hooks opt-in、全局卸载、rc/tag、正式发布或备份销毁授权。
- 本项交付：把本批 **release scope**、host/HOME 批次、非敏感 custodian、私有 `backup_root`、保留/复核安排和**拟**清理范围写成可回读记录。盘点只读；私有准备不等于批准 apply。
- 本配置源仓库不是默认迁移目标。未点名的用户项目、`~/.codex`、`~/.omp` 或其他 HOME **不得扫描**。Git author、OS 用户、lessons 分隔名 `640` 都不能当成 custodian。

## 2. Book Gate Plan

Gate Plan 只使用 `planned / running / passed / blocked / not-required`。各 reviewer 的结果词表写在独立审查节，不填进本表。

| Gate | 判定／触发事实 | 状态 |
|---|---|---|
| DDIA | required；本项冻结迁移范围、备份所有权、私有准备、保留门与恢复边界，设计稳定前必须审查 | passed |
| Legacy safety | 无既有行为缺陷修复，不改生产运行代码 | not-required |
| DDD | 无新领域术语或上下文归属变化 | not-required |
| Refactoring | 不改生产实现 | not-required |
| Release readiness | 本项不是 rc、v2.0.0 或真实部署完成 | not-required |
| grill-with-docs | 沿用已批准迁移边界，无新实质歧义 | not-required |

本仓库仍不创建 `.trellis/`、`ai/tasks/` 或 `.feature`。真实目标项目的新目录不自动变成本仓库目录。已记录用户政策原文（见第 3 节）。精确项目根与各 host 有效 HOME／Skill 根已写入私有层并回读（路径不进本文件）。旧 Trellis：用户原文存在，类别为仓库内 `.trellis` 目录；第 4 节只读盘点已确认该类别仍成立，细节只进私有层。当前会话环境变量不得自动当成真实 HOME。未点名 HOME 不扫描。第 4 节已开始并完成本轮只读记录；完整盘点不是 apply。

### 2.1 DDIA Data Design Review

```text
DDIA Data Design Review
Status: confirmed
Data owner and source of truth: 用户拥有真实项目树和 HOME。非敏感 custodian（用户原文，不得从 Git/OS/lessons 推断）拥有 backup_root、原件、候选、阶段/恢复收据和已登记副本。本冻结文件与主 PRD 只拥有可追踪协议层：逻辑名、host 选择、保留政策、拟清理类别、私有记录是否回读成功。目标项目绝对路径、HEAD、工作区状态与目录结构只进私有层。manifest 尚未存在；plan 之后才成为迁移绑定事实源。task.md 不因本项成为迁移 SoT。
Write / read / async / failure paths: 第 3 节字段全部齐后，才开始第 4 节授权盘点。点名本身不是盘点授权。字段未齐时不得访问目标项目任何路径，包括项目根 `AGENTS.md`；“先读 AGENTS.md”只是盘点开始后的第一步，不是提前读取许可。字段未齐时的项目读取是门禁外观察，不得填第 3 节当前值，不得计入第 6 节，也不得把精确路径或项目规则正文写入本文件或主 PRD。本源仓只写冻结协议与台账。私有准备记录只在用户给出删除范围外路径后写入；私有准备不等于 apply。任一项第 3 节字段缺失则保持 in-progress，不进 P2-02，不写项目/HOME。未点名路径不扫描。无队列/后台/跨服务异步。
Consistency model: 范围以用户原文为强一致输入。第 4 节盘点是字段齐后对点名路径的当时观察，不承诺之后不变。门禁外观察不是盘点，也不能当 apply 前态。共享 HOME/Skill 消费者要么整批纳入，要么明确隔离；未知消费者时全局卸载 not-allowed。原件指纹与敏感路径只留私有层。
Idempotency / ordering / retry / deduplication: 重复记录同一 pending 字段不是突变。同一项目点名两次不扩大扫描。拟清理清单不是 P2-05 清理确认，也不是 P3-04 销毁授权。cleanup/recovery/uninstall 永不删备份。
Schema / migration / backfill / rollback / replay: 本项不改迁移 schema、不 apply、不 backfill。它冻结后续 plan 必填 HITL 输入。实现变化后仍须重新 plan，不复用未绑定的旧盘点或门禁外观察当 apply 前态。回滚本项只需撤回协议文档；因尚未写用户数据，无数据回放。若在 manifest 前放弃，终止分支仍须在删除范围外私有记录保存并回读 custodian/归属/精确范围/独立授权/确认时间。
Observability and repair: 协议层表格显示 pending/已填，且只显示逻辑名。私有层保存精确路径/大小/checksum 并回读。回读失败视为未冻结。无后台催办；在 P2/P3 检查点由责任人复核，间隔 ≤30 天。
Required tests: 第 3 节齐后才开始第 4 节；盘点第一步才读项目根 `AGENTS.md`。证明本文件与主 PRD 不含目标项目绝对路径、HEAD、原件指纹、盘点结构或项目 `AGENTS.md` 正文。独立 review 无剩余 P0/P1。不把门禁外观察、提前读 `AGENTS.md` 或后续 apply/恢复演练算作本项通过。
```

本审查确认的是冻结协议本身可以稳定：缺字段就 fail-closed。它不是真实 apply、部署、cleanup、sync、tag 或备份销毁授权。已接受的原文见第 3 节；未齐字段保持 pending，不进 P2-02。

## 3. 本项必须冻结的字段

以下任一项缺失，P2-01 保持 in-progress，不进入 P2-02，不写项目／HOME。

| 字段 | 必须由用户提供 | 当前值 |
|---|---|---|
| 至少一个实际项目根 | 用户提供绝对路径与当前分支或 `detached:<full-sha>` 或明确非 Git；**精确路径只进私有层**，本表只记逻辑名与声明分支 | 已接受逻辑名 `demo`，声明分支 `main`（用户确认原文，当时非 git 实测）。精确路径、HEAD、工作区与远程跟踪不进本文件；**精确路径已写入私有层并回读**。第 4 节已只读打开该项目；HEAD／工作区只进私有层。**640-skills 不纳入。** |
| 每个项目的旧 Trellis 结构 | 用户说明或第 4 节授权盘点后记录；不猜版本。协议层只记类别／是否存在，细节进私有层 | **已接受用户原文，并经第 4 节只读盘点确认：** 存在。协议层类别：仓库内 `.trellis` 目录。版本与目录细节只进私有层。不得把门禁外观察抄入充数。 |
| 验证 host | Codex、OMP 或两者 | 已接受：两者 |
| 有效 HOME / Skill 根 | 每个 host 的实际配置根；共用者须整批纳入或明确隔离。精确路径只进私有层 | **政策已接受（用户原文「按你推荐和建议的执行」，用户事件未定时；11:01:55 仅为当时写入/记录时刻，不是用户消息发生时钟）：** `demo` 正式切换到新建的独立 Codex HOME、独立 OMP HOME、独立 Skill 根；当前共享 live HOME／Skill 根保持不动，且不作为 `demo` 的切换目标。P2-02 才在隔离副本＋已确认隔离根演练、不改 live；P2-02 获单独授权后再建目录和填充。P2-03 只对已冻结 `demo` 批次及隔离根迁移／停旧路由；共享 live HOME 上的旧工具不停用、不卸载。正式切换不降格为隔离演练。先前「冻结当前实际 Codex／OMP 根为正式目标」已作废。用户确认原文「HOME 候选都可用」（2026-09-22T17:06:54+08:00）仍是当时 live 候选回读，不是正式切换目标。**隔离根精确路径已由用户原文确认**（「隔离根用你建议的 TEMP 下独立目录，我确认，现在先别建」；观察时刻 2026-09-23T13:24:19+0800，不是独立的用户消息发生时钟）。确认的是此前 agent-proposed、`backup_root` 之外的 TEMP 独立目录；精确路径只进私有层；目录仍未创建。先前误建的空占位均已删除。P2-01 仍不创建正式 HOME。第 6 节第 3 项因此满足；第 6 节第 5 项无剩余 P0/P1。live 根精确路径仍在私有层，仅作第 4 节只读盘点对象。未扫描未点名 HOME。当前会话环境变量不得自动冻结。 |
| 共享批次闭包 | 同 HOME／Skill／全局入口的依赖项目名单，或隔离方案 | **已接受，并经后续隔离 HITL 裁决（用户事件未定时；11:01:55 仅为当时写入/记录时刻）：** 本批授权项目仅逻辑名 `demo`。用户先前原文「该实际 HOME 目前只服务 demo」不得写成盘点结论。第 4 节盘点见 Codex HOME hooks 状态有本批以外项目痕迹。用户原文「仍只迁 demo，那些痕迹忽略」（2026-09-22T17:48:03+08:00）：不扫描、不纳入、不扩大本批。用户原文「按你推荐和建议的执行」（用户事件未定时）：隔离政策为明确隔离 HOME／Skill 根，**不把当前共享 live HOME 改成隔离根**，live HOME 保持不动且不作为 `demo` 切换目标。不把 live HOME 改写成独用。正式隔离根不得放在 `backup_root` 内。第 6 节第 3 项已满足：用户已确认 `backup_root` 之外的 TEMP 独立目录；精确路径只进私有层；不是整批纳入，也不是已创建目录。未解共享痕迹 → 全局卸载仍 not-allowed。 |
| 未选平台 | Claude／Kimi 等只盘点、不修改；依赖未解前禁止全局卸载 | **已接受：** 只盘点、不修改；依赖未解前禁止全局卸载。 |
| 非敏感 custodian | 责任人标识；不从 Git/OS/lessons 推断 | 已接受用户原文：`kuno`。未从 Git/OS/lessons 推断。 |
| 私有 `backup_root` | 仓库外专用目录；不得与受管目标互相包含。精确路径只进私有层 | **已接受：** 在用户点名的 TEMP 父目录下新建专用子目录作为 `backup_root`；父目录本身不是 `backup_root`。通用下载目录仍拒绝。精确路径只进私有层，已回读。尚未复制原件。`backup_root` 只留备份／私有记录，**不得与受管目标互相包含**。正式隔离 HOME／Skill 根不得放进 `backup_root`。同一 TEMP 父目录下、`backup_root` 之外的独立目录已由用户原文确认；P2-01 仍不创建这些目录。先前误建的空占位已删除。精确路径只进私有层，标为 user-confirmed / not yet created。 |
| 最低保留与复核 | 默认：覆盖 P3 两周观察，并至少保留至正式发布后 14 天，取较晚者；首次及后续间隔 ≤30 天 | **已接受协议默认。** 不缩短。更严隐私／法律要求若冲突则停实际迁移并另定保全方案。 |
| 拟清理范围 | 项目旧目录／受管接线／全局旧工具分别列出；本批是否允许全局清理 | **已接受：** 本批只盘点、不删；`demo` 内受管旧条目待第 4 节授权盘点后才可列为 P2-05 候选；全局旧工具卸载 not-allowed；备份不随 cleanup 删除。本项同意不是 P2-05／P3-04 授权。 |
| 本配置源仓库 | 默认不纳入 | 已接受：不纳入 |
| 可选仓库外私有冻结文件 | 可选；只读用户点名的一份 | 已接受：不需要 |
| 私有准备记录位置 | 删除范围外、可写、可回读的既有私有记录或迁移报告路径。精确路径只进私有层 | **已接受并回读：** 建在专用 `backup_root` 内。不等于上面的可选冻结文件。精确路径不进本文件。 |

推荐澄清选项不能代替这些事实。第 3 节全部字段有用户原文后，才允许按第 4 节只读已点名路径；未点名路径始终不读。用户给出的精确路径写入私有层后，本文件只保留逻辑名。

### 3.1 本轮用户原文与裁决（2026-09-22）

1. 项目：逻辑名 `demo`，声明分支 `main`。未写该项目。精确路径不进本文件。用户确认原文「项目根候选可用，仍是 main」（2026-09-22T16:59:11+08:00），**已写入私有层并回读**。该回读是读私有记录，不是打开项目，不是第 4 节盘点。
2. host：Codex 与 OMP。
3. HOME／Skill 根政策：先前「冻结 `demo` 当前实际 Codex、OMP 配置根为本批正式目标」已由用户原文「按你推荐和建议的执行」（用户事件未定时；11:01:55 仅为当时写入/记录时刻）替换。`demo` 正式切换到新建的独立 Codex HOME、独立 OMP HOME、独立 Skill 根；当前共享 live HOME／Skill 根保持不动，且不作为 `demo` 的切换目标。P2-02 隔离演练不改 live；P2-02 获单独授权后再建目录和填充。精确路径不进本文件。隔离根精确路径已由用户原文确认并写入私有层（观察时刻 2026-09-23T13:24:19+0800，不是独立的用户消息发生时钟）。目录尚未创建。用户确认原文「HOME 候选都可用」（2026-09-22T17:06:54+08:00）仍是当时 live 候选回读，不是正式切换目标。该回读是读私有记录，不是打开或扫描这些配置目录，不是第 4 节盘点。未扫描未点名 HOME。
4. custodian：`kuno`。
5. `backup_root`：通用下载目录已拒绝。已接受在用户点名的 TEMP 父目录下新建专用子目录；父目录本身不是 `backup_root`。精确路径只进私有层，已回读。尚未复制原件。只留备份／私有记录，不得与受管目标互相包含。正式隔离根不在 `backup_root` 内。
6. 保留政策：已接受协议默认（P3 两周观察 ∪ 正式发布后 14 天，复核 ≤30 天）。
7. 拟清理：已接受本批只盘点；项目内受管旧条目待授权盘点后才可列为 P2-05 候选；全局卸载 not-allowed。
8. 640-skills：不纳入。
9. 可选私有冻结文件：不需要。
10. 共享闭包：本批仅 `demo`。用户先前「HOME 目前只服务 demo」不得写成盘点结论。未点名项目不扫描。第 6 节第 3 项已满足：用户已确认 `backup_root` 之外的 TEMP 独立目录；不是整批纳入，也不是已创建目录。第 6 节第 5 项无剩余 P0/P1。
11. 未选平台：只盘点、不修改；依赖未解前禁止全局卸载。
12. 私有准备记录：已建在专用 `backup_root` 内并回读；精确路径不进本文件。
13. 旧 Trellis：用户确认原文「demo 有旧 Trellis，我知道的是：在仓库内的 .trellis 目录下」（2026-09-22T17:12:23+08:00）。协议层只记存在／类别：仓库内 `.trellis` 目录。当时该说明不是第 4 节盘点。第 4 节只读盘点后仍确认该类别；细节只进私有层。不得把门禁外观察抄入充数。
14. 第 4 节后 HITL：用户原文「仍只迁 demo，那些痕迹忽略」（2026-09-22T17:48:03+08:00）。盘点所见 Codex HOME hooks 他项痕迹不扫描、不纳入、不扩大本批。不隔离 live HOME。不把先前「HOME 只服务 demo」改写成盘点结论。全局卸载仍 not-allowed。未 apply。
15. 未完成起步任务 HITL：用户原文「需要触碰这个任务」，预期迁移行为是发现 `00-bootstrap-guidelines` 则跳过该任务内容，理解其行为后模拟落地到新版本对应 spec 目录（用户事件当时未经 date 返回，保持未定时；原误记 18:11:00 已作废；18:26:22 仅为后来改写记录时间，不是用户事件时间）。裁决：本批不把该旧任务迁成 `ai/tasks/00-bootstrap-guidelines`，也不要求它的 handoff。该任务的行为是读仓库代码／既有约定后写入项目规范；共享目标是 `docs/spec/`。本批不以空模板直拷冒充已完成规范；原 `.trellis/spec/**` 仍私有保全。规范落地不在 P2-01 执行，也不写 live 项目；属后续已授权隔离演练或 apply 窗口。此条只解除该起步任务因缺 handoff 造成的相关迁移 blocked。当前 v2 新项目 `init` 不会自动做同类 spec 落地；仅用户明确要求规范初始化时才有条件任务。本条不把该事实改成新需求。独立 review 未做。未 apply。
16. 共享入口隔离 HITL：用户原文「按你推荐和建议的执行」（用户事件未定时；11:01:55 仅为当时写入/记录时刻，不是用户消息发生时钟）。采纳推荐：本批仍仅 `demo`；盘点他项 traces 不扫描、不纳入、不扩大。`demo` 正式切换到新建的独立 Codex HOME、独立 OMP HOME、独立 Skill 根。当前共享 live HOME／Skill 根保持不动，且不作为 `demo` 的切换目标。不把 live HOME 改写成独用。全局卸载仍 not-allowed。此条只冻结隔离政策（相对 live HOME）。精确隔离根路径未经用户确认。当时不得声称第 6 节第 3 项已满足，也不得把 11:01:55 当成用户事件时间。不在 live HOME 写入。未 apply。
17. 非-HITL 内部校正（折叠；不是用户原文、不是 HITL、不是用户授权证据）：Agent 曾误建空正式 HOME、误把 watchdog 建议写成用户原文、误把同一会话复审当成独立 review。最终事实（记录时刻 2026-09-23T11:37:51+08:00）：精确隔离根路径为 agent-proposed / pending user confirmation（用户未确认具体路径；目录未创建；不得写入 `backup_root`）；第 6 节第 3 项未满足；第 6 节第 5 项独立 review 为 blocked/unavailable。细节只留私有审计记录。未 apply。不把 P2-01 标 done。
18. 隔离根路径确认与审查校正：用户原文「隔离根用你建议的 TEMP 下独立目录，我确认，现在先别建」。观察时刻 2026-09-23T13:24:19+0800，是处理该原文时的 date 返回，不是独立的用户消息发生时钟。确认此前 agent-proposed、且在 `backup_root` 之外的 TEMP 独立目录。精确路径只进私有层。目录未创建。不是 P2-02 建目录或填充授权。未 apply。同条非用户原文：本会话对 `bc27e2d` 的审查漏了协议层盘点细节，上一条「没有剩余必须先改的问题」不作为第 6 节第 5 项通过。Skill 名、MCP 配置键、插件锁和根目录 MCP 文件有无应从协议层收回。其后主 PRD 台账仍留有 Skill 根名称；本审查已从台账收回，具体名称只留私有层。身份是否存在、受管接线类别仍留协议层。收回后的快照须另一会话再审。不把 P2-01 标 done。

隔离根路径已确认，目录未创建。精确项目根与各 host live HOME／Skill 根已私有回读。旧 Trellis 已有用户原文（存在；类别为仓库内 `.trellis` 目录），第 4 节只读盘点已确认该类别。第 3 节字段均有用户原文；有效 HOME 的精确隔离根路径已确认，目录仍未创建。第 4 节已开始并完成本轮只读记录（私有层）。§6.3 已满足：本批仍只迁 `demo`、盘点他项 traces 忽略（不扫描、不纳入）；隔离根为用户确认的、`backup_root` 之外的 TEMP 独立目录；当前共享 live HOME 保持不动且不作为切换目标；全局卸载仍 not-allowed。先前误建的空占位已删除。`00-bootstrap-guidelines` 缺 handoff 的相关迁移 blocked 已由用户书面裁决替换为：跳过该任务内容、后续已授权窗口模拟 spec 落地。第 6 节第 5 项无剩余 P0/P1：2026-09-23T15:00:59+0800 记录对 14:08:30 两份未提交文档的只读审，未再改盘点句。台账于 2026-09-23T15:31:42+0800 标 done。任务 PR #71 已合并，merge `4cf711a299ff5b056a9a8afb78d381363100fb9a`。未 apply、未 sync、未清理。曾有门禁外项目根读取（§3.2），不是盘点，不得抄入充数。

### 3.2 门禁外观察（不得推进 P2-01）

2026-09-22 在 HOME/Skill 根、共享闭包、未选平台、`backup_root`、保留与拟清理仍 pending 时，曾读取点名项目 `demo` 的项目根。该读取早于第 4 节，**不是盘点**。当时取得的精确路径、HEAD、工作区状态与目录结构：

- 不得写入第 3 节当前值，也不得留在本文件或主 PRD
- 不得满足第 6 节第 2 项
- 不得作为拟清理清单或共享闭包证据
- 字段齐后必须按第 4 节重新盘点；精确路径只进私有层；不得复用本次观察当 apply 前态

未写该项目。未扫描未点名 HOME。当时观察未写入第 3 节当前值，也不进本文件或主 PRD。

本轮第 3 节字段均有用户原文。用户原文「开始第 4 节盘点」（2026-09-22T17:20:54+08:00）后已执行第 4 节。若第 3 节再次出现 pending，不得继续访问目标项目。项目规则正文不进本文件。

## 4. 只读盘点规则（字段齐后才执行）

第 3 节任一字段 pending 时，不得执行本节，也不得访问目标项目任何路径，包括项目根 `AGENTS.md`。用户点名项目根不等于盘点授权。门禁外观察不得填入第 3 节当前值，不得计入第 6 节验收。

授权盘点开始时（第 3 节已齐），对每个目标项目先读项目根 `AGENTS.md`；若它声明更深层 `AGENTS.md` 优先，再只读那些已证明存在的更深路径。未读项目根 `AGENTS.md` 不得继续访问该项目其他路径。

对每个已授权项目根，分开记录，不混成“仓库很干净”：

1. Git tracked 内容、当前完整 SHA、工作区是否 dirty。
2. ignored / untracked：`.trellis`、task.json、jsonl、workspace journal、身份、报告、缓存。
3. 项目内平台产物所有权：`.codex`、`.omp` 等受管旧条目 vs 用户自定义。
4. 声明的 HOME／Skill 根里、与本批项目相关的 MCP／hooks／全局 Skill／包版本；未授权根不读。
5. 未完成 workspace 上下文：缺手批准 handoff 的项目在相关迁移前保持 blocked（PLAN-008 已修代码缺口，真实项目仍要有交接／恢复证据）。
6. 身份：只报告 `.trellis/.developer` 与 `.sbtd/developer` 是否存在及是否合法，不改写。

盘点输出分两层：

- **可追踪协议层**（本文件／主 PRD）：非敏感决定、项目逻辑名、声明分支、host 选择、保留政策、拟清理类别、私有记录是否已回读。禁止写入目标项目绝对路径、HEAD、工作区状态、目录结构或原件指纹。
- **私有层**（`backup_root` 或用户指定准备记录）：精确路径、大小、checksum、敏感结构、publication_decisions 候选。原件指纹不进共享仓库、公共报告或公开日志。

### 4.1 本轮只读盘点（协议层摘要）

用户原文「开始第 4 节盘点」（2026-09-22T17:20:54+08:00）。逻辑项目 `demo`。细节、路径、HEAD、工作区 porcelain、目录结构和指纹只进私有层。

- 第一步已读项目根 `AGENTS.md`（正文不进本文件）。它声明更深 `AGENTS.md` 优先；按名查找无更深文件。
- Git／ignored／untracked／身份／项目内 `.codex` `.omp`／已确认 HOME 范围内与本批相关的 Skill／hooks／插件，已写入私有记录并回读。
- 旧 Trellis：盘点确认存在；协议层类别仍为仓库内 `.trellis` 目录。
- 身份：`.trellis/.developer` 存在；`.sbtd/developer` 不存在。未改写。合法性与格式细节只进私有层。
- 项目内有受管 Trellis 接线（`.codex`／`.omp`／项目级 `trellis-*` Skill）。这是盘点观察，不是 P2-05 清理授权。
- 已确认 HOME／Skill 根：只读，未点名根不读。与本批相关的全局 Skill、MCP 配置键、插件锁和根目录 MCP 文件有无，已写入私有记录并回读；具体名称不进协议层。不得从本文件推断两套 HOME 根都有同一 MCP。Codex HOME 的 hooks 状态里还有本批以外项目痕迹。用户原文「仍只迁 demo，那些痕迹忽略」（2026-09-22T17:48:03+08:00）：**不扫描、不纳入、不扩大本批**；当时不是隔离 live HOME，也不是把 HOME 改写成独用。全局卸载仍 not-allowed。路径确认见第 3.1 条第 18 点。本条只记录当时 live HOME 已盘点并回读。
- 盘点见未完成起步任务 `00-bootstrap-guidelines`、无 handoff。用户原文「需要触碰这个任务」及预期迁移行为（用户事件未定时；不得用 18:26:22 充当用户事件时间）：发现该任务则跳过其内容；理解其行为是读仓库代码后落地规范，并在后续已授权窗口模拟写入 `docs/spec/`。此条只解除该任务因缺 handoff 造成的相关迁移 blocked。不在 P2-01 写项目。不把 P2-01 标 done，也不授权 apply。其他未完成 workspace 上下文仍按第 4 节第 5 项 blocked。
- 用户原文「按你推荐和建议的执行」（用户事件未定时；不得用 11:01:55 充当用户事件时间）：隔离政策为 `demo` 正式切换到新建独立 HOME／Skill 根；当前共享 live HOME／Skill 根保持不动、不是切换目标。
- 隔离根路径已由用户原文确认，目录未创建。本会话对 `bc27e2d` 的审查不作为第 6 节第 5 项通过。细节只留私有层。未写 live HOME。未 apply。
- 读 `AGENTS.md` 本身不是完整第 4 节；本轮已按第 4 节 1–6 项只读记录。未复制原件。未 apply。

## 5. 明确不做

- 不 `migration apply` / `verify` / `cleanup`，不带迁移上下文的 `init`／`init-projects`。
- 不 sync live automation，不安装或卸载真实 Graft／Trellis／GitNexus，不改 hooks。
- 不创建 tag，不缩短保留窗，不删除备份。
- 不把干净 `git status`、现有 tag 或目录存在当成完整备份。
- 不把本项同意当成 P2-05 清理确认或 P3-04 销毁确认。
- 不把门禁外项目读取当成第 4 节盘点、第 3 节 Trellis 结构冻结或拟清理证据。
- 第 3 节未齐时不访问目标项目任何路径，包括 `AGENTS.md`。
- 授权盘点开始后，不在未读该项目根 `AGENTS.md` 的情况下继续访问其其他路径。读 `AGENTS.md` 本身仍不是完整第 4 节盘点。
- 不把目标项目绝对路径、HEAD、工作区状态或盘点结构写入本文件或主 PRD。
- 不把正式隔离 HOME／Skill 根放进 `backup_root`；`backup_root` 不得与受管目标互相包含。
- P2-01 不创建正式隔离 HOME／Skill 根目录。路径已由用户原文确认，仍不建。建目录和填充属 P2-02 另一次授权。
- 同一实现会话的只读复审不得称为独立 review，也不得据此写「无剩余文档 P0／P1」。修正协议层盘点细节的会话不得把修正前的审查结论写成第 6 节第 5 项通过。第 6 节第 5 项在不同 reviewer／会话实际审过该快照前保持 blocked/unavailable。

## 6. 验收

P2-01 标 done 前必须同时成立：

1. 第 3 节字段均有用户原文；custodian、`backup_root`、拟清理范围、保留安排和目标项目精确路径可在私有记录中回读。本文件只保留逻辑名。
2. 每个目标项目有只读盘点：tracked／untracked、身份、声明 HOME 范围内的共享消费者。该盘点必须发生在第 3 节齐之后；盘点第一步才读项目根 `AGENTS.md`。门禁外观察和第 3 节未齐时的任何项目读取都不算盘点。
3. 共享入口要么整批纳入，要么有明确隔离 HOME／Skill 根；未解依赖则全局卸载为 not-allowed。
4. 用户书面接受 §11.8 最低保留，或因更严要求停止迁移并另定保全方案。
5. 独立 review 无剩余 P0／P1；生产代码本项默认不改。

完成后仍不解锁真实 apply。P2-02 要另一次授权，才能在隔离副本和隔离 HOME 演练。
